# Usage

Build through the workspace:

```bash
yarn workspace @neurondev/ndx-core build
```

App wrappers import `@neurondev/ndx-core/cli` and call `main()`. Direct user
execution should use the `@neurondev/ndx` bins.

