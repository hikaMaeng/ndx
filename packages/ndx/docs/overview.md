# Overview

`@neurondev/ndx-core` owns ndx CLI, server, dashboard, model, runtime, tool,
process, config, and shared protocol logic. App packages import its public
subpaths and do not own domain behavior.

