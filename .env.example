# ndx no longer reads runtime configuration from .env.
# Put global settings in /home/.ndx/settings.json and project settings in .ndx/settings.json.
# Keep secrets such as provider keys, Tavily keys, GitHub tokens, Docker Hub tokens, and npm tokens out of repository files.
