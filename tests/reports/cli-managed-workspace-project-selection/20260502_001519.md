# Test Report: cli-managed-workspace-project-selection

## Timestamp

2026-05-02 00:15:19 Asia/Seoul

## Environment

- Repository: `/mnt/f/dev/ndx`
- Shell: bash
- Runtime: Yarn PnP with Node.js test runner

## Executed Steps

1. Ran `yarn build`.
2. Ran `node --test dist/tests/cli-workspace.test.js dist/tests/config.test.js dist/tests/tools.test.js dist/tests/session-server.test.js dist/tests/cli-session-client.test.js`.
3. Ran `yarn test`.

## Observed Logs

- Targeted test run: 33 tests passed, 0 failed.
- Full `yarn test`: 55 tests passed, 0 failed.
- The targeted run covered server-address normalization, managed Docker compose
  generation, socket-first attach, `.ndx/system` core tool discovery, project
  folder create/list APIs, and session behavior.

## Failures

- `npm run deploy` was not run in this local verification because this deploy
  path clones the current pushed Git branch from GitHub. The local branch
  changes were unpushed at verification time, so deploy would not verify these
  changes.

## Reproducibility

Run from `/mnt/f/dev/ndx`:

```bash
yarn build
node --test dist/tests/cli-workspace.test.js dist/tests/config.test.js dist/tests/tools.test.js dist/tests/session-server.test.js dist/tests/cli-session-client.test.js
yarn test
```

## Locator Outcome

Not applicable; this behavior has no browser surface.
