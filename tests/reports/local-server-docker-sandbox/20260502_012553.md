# Test Report: local-server-docker-sandbox

## Timestamp

2026-05-02T01:25:53+09:00

## Environment

- Workspace: `/mnt/f/dev/ndx`
- Branch: `codex/bootstrap`
- Runtime: Node.js 22.22.2, Yarn 4.14.1, Docker Compose
- Sandbox image: `hika00/ndx-sandbox:0.1.0`

## Executed Steps

1. Ran `yarn build`.
2. Ran `yarn test`.
3. Ran Prettier over changed docs and TypeScript files. Dockerfile was skipped
   because Prettier has no inferred parser for it.
4. Ran `npm run deploy`.
5. Ran `docker push hika00/ndx-sandbox:0.1.0`.
6. Ran `docker pull hika00/ndx-sandbox:0.1.0`.
7. Started a local `SessionServer` with `requireDockerSandbox: true` and the
   pushed image tag.
8. Fetched the local dashboard URL and removed the generated sandbox container.
9. After adding image-pull fallback in `ensureDockerSandbox` and the
   unauthenticated-ignore test, reran `yarn test`.
10. Reran `npm run deploy`, pushed the final rebuilt sandbox image, pulled the
    pushed tag, and repeated server verification.

## Observed Logs

- `yarn build`: passed.
- `yarn test`: passed with 56 tests before deploy and after the image-pull and
  authentication-ignore changes.
- `npm run deploy`: passed. It built and tested locally, removed prior compose
  resources, built `hika00/ndx-sandbox:0.1.0`, started `ndx-sandbox`, wrote the
  verification file in `/workspace/tmp`, and removed the compose container and
  network.
- Final `npm run deploy`: passed after the final code/test changes.
- Final Docker push: `0.1.0` pushed with digest
  `sha256:b4a50db87ae1c9d6852c25a1a2d48039eefc88a5dfdb566df60417f819e8867d`.
- Pushed-tag server verification:

```json
{
  "socket": "ws://127.0.0.1:46641",
  "dashboard": 200,
  "sandbox": "ndx-sandbox-f8bb21bade0321a3"
}
```

## Failures

- First pushed-tag verification script attempt failed before server startup due
  to a cleanup-block syntax error in the ad hoc Node script. The corrected
  script passed.
- Prettier reported no parser for `Dockerfile`; this is a tooling limitation,
  not a source formatting failure.

## Reproducibility

Run:

```bash
yarn build
yarn test
npm run deploy
docker push hika00/ndx-sandbox:0.1.0
docker pull hika00/ndx-sandbox:0.1.0
```

Then start a `SessionServer` with `requireDockerSandbox: true` and
`tools.dockerSandboxImage` set to `hika00/ndx-sandbox:0.1.0`.

## Locator Outcome

Dashboard verification used HTTP fetch against the local dashboard URL returned
by `SessionServer.listen`. The response status was 200. The durable dashboard
locator contract remains documented in `docs/testing.md`; no dashboard markup
contract changes were made in this task.
