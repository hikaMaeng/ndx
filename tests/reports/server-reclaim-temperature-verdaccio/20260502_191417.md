# Test Report: server-reclaim-temperature-verdaccio

## Timestamp

2026-05-02 19:14:17 KST

## Environment

- Workspace: `/mnt/f/dev/ndx`
- Branch: `codex/dashboard-webserver`
- Package version: `0.1.5`
- Verdaccio: `https://verdaccio.neurondev.net/`
- Install prefix: `/tmp/ndx-install-verify`
- Installed project: `/tmp/ndx-installed-project`

## Executed Steps

1. Ran `yarn build`.
2. Ran `node --test dist/tests/model-adapters.test.js dist/tests/config.test.js dist/tests/cli-workspace.test.js`.
3. Ran `yarn test`.
4. Ran `npm run deploy`.
5. Ran `npm publish --registry https://verdaccio.neurondev.net/`.
6. Installed `@neurondev/ndx@0.1.5` with `npm install -g --prefix /tmp/ndx-install-verify --registry https://verdaccio.neurondev.net/`.
7. Verified `/tmp/ndx-install-verify/bin/ndx --version` and `/tmp/ndx-install-verify/bin/ndxserver --version`.
8. Created stale container `ndx-stale-verify` with ndx owner labels.
9. Started installed `ndxserver` with Docker sandboxing enabled for `/tmp/ndx-installed-project`.
10. Verified stale container removal and current workspace sandbox labels.
11. Stopped the installed server through `POST /api/exit` and removed remaining ndx-owned sandbox containers.

## Observed Logs

- `yarn build`: passed.
- Focused tests: 23 passed.
- `yarn test`: 59 passed, 0 failed.
- `npm run deploy`: passed; build and tests passed, Compose removed prior resources, rebuilt `hika00/ndx-sandbox:0.1.0`, started `ndx-sandbox`, wrote `/workspace/tmp/ndx-docker-verify.txt`, and tore Compose down.
- `npm publish`: published `@neurondev/ndx@0.1.5` to Verdaccio.
- Installed binary versions:
  - `ndx --version`: `0.1.5`
  - `ndxserver --version`: `0.1.5`
- Installed binary target:
  - `/tmp/ndx-install-verify/lib/node_modules/@neurondev/ndx/dist/src/cli/main.js`
- Installed server started:
  - `[session-server] ws://127.0.0.1:45332`
  - `[dashboard] http://127.0.0.1:45333`
- Current sandbox labels:
  - `/ndx-tool-ndx-installed-project|ndx-server|tool-sandbox|/tmp/ndx-installed-project|hika00/ndx-sandbox:0.1.0`

## Failures

None in the final verification path.

## Reproducibility

```bash
yarn build
node --test dist/tests/model-adapters.test.js dist/tests/config.test.js dist/tests/cli-workspace.test.js
yarn test
npm run deploy
npm publish --registry https://verdaccio.neurondev.net/
npm install -g @neurondev/ndx@0.1.5 --registry https://verdaccio.neurondev.net/ --prefix /tmp/ndx-install-verify
/tmp/ndx-install-verify/bin/ndx --version
/tmp/ndx-install-verify/bin/ndxserver --version
```

## Locator Outcome

No browser UI changed in this task. Dashboard browser locator verification
remains recorded in `tests/reports/dashboard-webserver/20260502_181812.md`.
