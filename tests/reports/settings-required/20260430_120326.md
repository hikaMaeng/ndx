# Test Report: settings-required

## Timestamp

2026-04-30T12:03:26+09:00

## Environment

- Workspace: `/mnt/f/dev/ndx`
- Shell: bash
- Command: `npm test`

## Executed Steps

1. Built TypeScript with `tsc -p tsconfig.json` through `npm test`.
2. Ran all compiled Node test files with `node --test dist/tests/*.test.js`.
3. Exercised config loader coverage for global/project cascading, missing
   settings failure, and bootstrap behavior.
4. Exercised session server bootstrap coverage through the full test suite.
5. Pushed `codex/fixmodelsetting` so `npm run deploy` could clone the current
   branch through Docker.
6. Ran `npm run deploy`; it completed the host build, compose down, no-cache
   Docker image build, and started the in-container `npm test`.
7. Stopped the deploy run after the in-container test process remained active
   for several minutes after subtest 32, then removed the run container and
   brought the compose project down.

## Observed Logs

- `# tests 41`
- `# pass 41`
- `# fail 0`
- `# duration_ms 21614.917054`
- Docker build cloned `https://github.com/hikaMaeng/ndx.git@codex/fixmodelsetting`.
- In-container deploy test output reached `ok 32 - session ownership uses last
  prompt attempt across socket servers` before the process stopped producing
  output.
- Cleanup removed `ndx-ndx-agent-run-03db75c684de` and `ndx_default`.

## Failures

Host `npm test` had no failures. `npm run deploy` did not complete: the
in-container `npm test` hung after subtest 32 and was manually stopped, so the
deploy script did not reach the final mock CLI verification command.

## Reproducibility

Run `npm test` from `/mnt/f/dev/ndx`. To reproduce the deploy result, run
`npm run deploy` from branch `codex/fixmodelsetting`.

## Locator Outcome

No browser UI exists for this package.
