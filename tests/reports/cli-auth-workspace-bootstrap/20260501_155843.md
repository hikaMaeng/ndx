# Test Report: cli-auth-workspace-bootstrap

## Timestamp

2026-05-01 15:58:43 +0900

## Environment

- Workspace: `/mnt/c/Users/hika0/.codex/worktrees/5fa7/ndx`
- Shell: bash
- Node/Yarn: repository `packageManager` uses Yarn 4.14.1

## Executed Steps

1. Ran `yarn install --mode=update-lockfile` after changing the package name to `@neurondev/ndx`.
2. Ran `yarn install --immutable`.
3. Ran `yarn build`.
4. Ran `yarn test`.
5. Added the server social-login test and reran `yarn build`.
6. Reran `yarn test`.
7. Pushed `codex/auth-install-bootstrap` because deploy clones the current Git branch from GitHub.
8. Ran `npm run deploy`.

## Observed Logs

- `yarn install --mode=update-lockfile`: completed and removed the old `ndx@workspace:.` lockfile entry.
- `yarn install --immutable`: completed with Yarn's expected Plug'n'Play ESM loader warning.
- `yarn build`: passed after formatting.
- `yarn test`: passed 53 tests, including CLI last-login switching, workspace bootstrap compose-state generation, and social login account creation.
- `npm run deploy`: passed. Docker removed prior compose resources, built
  `ndx-agent:local` from
  `https://github.com/hikaMaeng/ndx.git@codex/auth-install-bootstrap`, ran 53
  in-container tests, executed the in-container mock agent, wrote `verified` to
  `/workspace/tmp/ndx-docker-verify.txt`, and removed the compose network.

## Failures

- Initial `yarn build` failed because the package rename required a lockfile refresh. The lockfile was updated with Yarn.

## Reproducibility

Run:

```bash
yarn install --immutable
yarn build
yarn test
npm run deploy
```

Set `NDX_CLI_STATE_DIR` in tests that need isolated host CLI app state.

## Locator Outcome

No new browser UI was added. Existing dashboard placeholder locator contract is
unchanged and remains covered by the session-server dashboard placeholder test.
