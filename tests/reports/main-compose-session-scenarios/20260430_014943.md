# Test Report: main-compose-session-scenarios

## Timestamp

2026-04-30 01:49:43 KST

## Environment

- Workspace: `/tmp/ndx-main-compose-update`
- Branch: `codex/main-compose-session-validation`
- Main commit under test: `cb56493f3256c4f777b92aafff26baed67f92831`
- Docker image: `ndx-agent:local`
- Image id: `sha256:7b907be8af8f538520dcb3ac4b865962e333a28ac87dc3eed16448cbfbea9a16`
- Running service: `ndx-main-compose-update-ndx-agent-1`

## Executed Steps

1. Ran `docker compose down --remove-orphans`.
2. Ran `NDX_GIT_REF=main docker compose build --no-cache ndx-agent`.
3. Ran `docker compose up -d ndx-agent`.
4. Confirmed the running container reports:
   - commit `cb56493f3256c4f777b92aafff26baed67f92831`
   - branch `main`
   - Node `v22.22.2`
5. Copied `tests/session-image-scenarios.mjs` into the running container with
   `docker compose cp`.
6. Ran `docker compose exec -T --workdir /opt/ndx ndx-agent node /tmp/session-image-scenarios.mjs`.
7. Ran `docker compose exec -T --workdir /opt/ndx ndx-agent npm test`.
8. Confirmed `docker compose ps` shows `ndx-agent` still running.

## Observed Logs

Session scenario output:

```json
{
  "ok": true,
  "deletion": {
    "createdNumbers": [1, 2, 3],
    "restoredNumber": 1,
    "deleteCandidates": [2, 3],
    "remainingNumbers": [1, 3],
    "staleDeleteNotification": "session/deleted"
  },
  "replacement": {
    "restoredNumber": 1,
    "staleOutputPersisted": false,
    "ownerCanContinue": true
  }
}
```

In-container `npm test`:

- Passed 40 tests.
- Covered CLI `restoreSession` command behavior.
- Covered CLI delete prompt behavior.
- Covered session server restore, delete, stale-owner termination, and ownership
  replacement tests.

Compose status:

- `ndx-main-compose-update-ndx-agent-1` is `Up`.

## Failures

- First ad hoc stdin script attempt failed because `node --input-type=module`
  was inherited by the forked session log writer. The committed scenario file
  avoids that and passed.
- Two script expectation fixes were made before the successful run:
  public restore responses expose `number` instead of internal `sequence`, and
  delete candidate filtering requires `currentSessionId`.

## Reproducibility

Run:

```bash
docker compose down --remove-orphans
NDX_GIT_REF=main docker compose build --no-cache ndx-agent
docker compose up -d ndx-agent
docker compose cp tests/session-image-scenarios.mjs ndx-agent:/tmp/session-image-scenarios.mjs
docker compose exec -T --workdir /opt/ndx ndx-agent node /tmp/session-image-scenarios.mjs
docker compose exec -T --workdir /opt/ndx ndx-agent npm test
docker compose ps
```

## Locator Outcome

Not applicable. The package has no browser UI.
