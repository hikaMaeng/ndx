# Test Report: session-context-commands

## Timestamp

2026-05-03T17:20:57+09:00

## Environment

- Workspace: `/mnt/c/Users/hika0/.codex/worktrees/ea06/ndx`.
- Branch: `codex/addcontextcommand`.
- Package: `@neurondev/ndx@0.1.14`.
- Runtime: Node.js 22.22.2, Yarn 4.14.1.
- Browser: Playwright 1.59.1 headless Chromium.
- Docker target: `hika00/ndx-sandbox:0.1.0`.

## Executed Steps

1. Ran `yarn install --immutable`.
2. Ran `yarn build`.
3. Ran `yarn test`.
4. Ran `npm run deploy`.
5. Started `node dist/src/cli/main.js serve --mock --cwd
/mnt/c/Users/hika0/.codex/worktrees/ea06/ndx --listen 127.0.0.1:45131
--dashboard-listen 127.0.0.1:45132`.
6. Fetched `http://127.0.0.1:45132/dashboard` and checked semantic anchors by
   HTML pattern.
7. Opened `http://127.0.0.1:45132/dashboard` in headless Chromium and verified
   role/name/test-id locators.
8. Stopped the local server with Ctrl+C.

## Observed Logs

- `yarn install --immutable`: completed with the expected Yarn PnP ESM loader
  warning.
- `yarn build`: completed successfully.
- `yarn test`: `65` tests passed, `0` failed.
- `npm run deploy`: completed successfully for `@neurondev/ndx@0.1.14` after
  build/test, `docker compose down --remove-orphans`, `docker compose build
--no-cache ndx-sandbox`, `docker compose up -d ndx-sandbox`, sandbox shell
  verification, and compose teardown.
- Dashboard HTML check returned `10685` bytes and found `ndx-dashboard`,
  `Server Dashboard`, `Dashboard menu`, `Server actions`, `Reload`, `Exit`,
  `role="status"`, `dashboard-sources`, and `dashboard-bootstrap`.
- Playwright output:

```json
{
  "title": "NDX Dashboard",
  "statusCount": 1,
  "url": "http://127.0.0.1:45132/dashboard"
}
```

## Failures

- First Playwright attempt used `require()` with top-level `await` and failed
  with `ERR_AMBIGUOUS_MODULE_SYNTAX`.
- Second Playwright attempt imported `playwright` by package name and failed
  because Playwright is installed globally, not in this package's PnP
  dependency graph.
- Final Playwright run imported the globally installed module by absolute path
  and passed.

## Reproducibility

Run the commands from the workspace root. If Playwright is not in the package
dependency graph, import the local global module path or add a repo-owned
browser test dependency before turning this into a committed automated browser
test.

## Locator Outcome

The dashboard locator contract passed with role/name selectors for heading,
navigation, `Reload`, and `Exit`, plus `data-testid` selectors for
`ndx-dashboard`, `dashboard-sources`, and `dashboard-bootstrap`.
