# Test Report: session-server
## Timestamp
2026-04-29 03:29:48 KST

## Environment
- Workspace: `/mnt/f/dev/ndx`
- Shell: bash
- Node: repository `npm test` path
- Docker: `npm run deploy`

## Executed Steps
1. Ran `npm test`.
2. Ran `node dist/src/cli.js --mock "list files"`.
3. Ran `npm run deploy`; first attempt failed on Docker Hub metadata EOF, second attempt passed.

## Observed Logs
- `npm test`: 15 tests passed, including `session server owns thread events, subscribers, and JSONL persistence`.
- CLI smoke: command completed through the embedded session server and printed `mock agent completed`.
- CLI smoke warning: persistence was disabled because `/home/.ndx/sessions/ts-server` could not be created in the host environment.
- `npm run deploy`: local TypeScript build passed, Docker image built, in-container tests passed, in-container mock agent command wrote `tmp/ndx-docker-verify.txt`, compose network was removed.

## Failures
- Initial `npm run deploy` attempt failed while resolving `node:22-bookworm-slim` from Docker Hub with `EOF`.
- Host CLI smoke could not persist to `/home/.ndx` because the directory was not writable; the server isolated the persistence failure and kept the turn running.

## Reproducibility
- Re-run `npm test` for local integration coverage of the new session server.
- Re-run `npm run deploy` for Docker build, in-container tests, and mock execution.
- The deploy Dockerfile clones the current Git branch from GitHub, so Docker coverage reflects the remote branch state.

## Locator Outcome
No browser UI exists for this package.
