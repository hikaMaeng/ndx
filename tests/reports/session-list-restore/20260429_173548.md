# Test Report: session-list-restore

## Timestamp

2026-04-29 17:35:48 Asia/Seoul

## Environment

- Workspace: `/mnt/f/dev/ndx`
- Branch: `codex/sessionlistrestore`
- Shell: bash

## Executed Steps

1. Added server-side session listing and restore support backed by JSONL
   persistence.
2. Added CLI command handling so `/restore` switches the active session.
3. Added integration coverage for live `/session`, restore by number, cold
   server restore, continued JSONL append, and CLI active-session switching.
4. Ran `npm run build`.
5. Ran `npm test`.
6. Pushed `codex/sessionlistrestore` and ran `npm run deploy`.
7. Renamed the public control surface from thread-oriented operations to
   session-oriented operations.
8. Added first-prompt session numbering/title persistence and owner-file
   reclaim behavior.
9. Added a two-socket-server race test that blocks the first server mid-turn,
   restores the same session from a second server, then verifies stale
   in-flight output is absent from JSONL and `session/read`.
10. Increased the existing abort-propagation file wait from 2s to 5s after it
    proved timing-sensitive under the expanded full suite.

## Observed Logs

- `npm run build`: passed.
- `npm test`: passed, 34 tests.
- `npm run deploy`: passed.
- Docker image build cloned
  `https://github.com/hikaMaeng/ndx.git@codex/sessionlistrestore`.
- Docker `npm test`: passed, 34 tests.
- Docker mock agent completed and wrote `tmp/ndx-docker-verify.txt` with
  `verified`.

## Notes

Latest deploy verification completed without manual intervention after the
session ownership race coverage was added.

## Reproducibility

Run:

```bash
npm test
```
