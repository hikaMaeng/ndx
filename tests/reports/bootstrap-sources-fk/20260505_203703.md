# Bootstrap Sources And SQLite FK Report

## Changes Under Test

- `initialize` now returns loaded source paths plus context-source metadata.
- CLI startup renders `[sources]` groups for settings, AGENTS.md, and skills.
- SQLite session schema version 3 references `users.userid` from
  `session.userid`.
- Existing v2 session tables are migrated in place, preserving row ids and
  event references while normalizing session user ids.

## Executed

1. `yarn install --immutable`
2. `yarn build`
3. `node --test dist/tests/cli-session-client.test.js`
4. `node --test dist/tests/session-server.test.js --test-name-pattern "SQLite v2 account rows|session server owns session events"`
5. `node --test dist/tests/config.test.js --test-name-pattern "AGENTS|skills"`
6. `node --test dist/tests/runtime.test.js --test-name-pattern "instruction sources|skill tool"`
7. `yarn test`
8. `npm run deploy`
9. `npm publish --registry https://verdaccio.neurondev.net/`
10. `npm install --prefix /tmp/ndx-install-verify-032 @neurondev/ndx@0.1.32 --registry https://verdaccio.neurondev.net/`
11. Installed `ndx --mock --cwd /tmp/ndx-scenario-032 ...`
12. Installed `ndxserver serve --mock --cwd /tmp/ndx-command-scenario-032 ...`
13. Installed package JSON-RPC command scenario for `initialize`, `turn/start`,
    `/init`, `/context`, `/events`, and `/session`.
14. Dashboard HTML locator check with `curl` for `dashboard-sources` and
    `dashboard-bootstrap`.

## Results

- Targeted CLI test passed and printed `[sources] 4` with AGENTS.md and skills
  groups.
- Targeted session-server run passed, including the v2 account FK migration
  regression.
- Targeted config and runtime tests passed, preserving AGENTS.md/skills cascade
  behavior and context-source token grouping.
- Full `yarn test` passed: 87 tests.
- `npm run deploy` passed: build, full tests, compose resource removal,
  no-cache sandbox build, sandbox start, verification file write, and compose
  teardown completed.
- Verdaccio publish succeeded for `@neurondev/ndx@0.1.32`.
- Installed `ndx` startup printed `[sources] 6` with settings, two project
  AGENTS.md files, user AGENTS.md, project skill, and user system skill.
- The installed first-turn scenario completed without `FOREIGN KEY constraint
failed`; the migrated database reported `user_version = 3` and
  `foreignKeyErrors = 0`.
- Installed `ndxserver` loaded the same six sources and served the dashboard.
- Installed command scenario confirmed `/init` includes `SKILL.md`, `/context`
  includes `agents_project` and `skills_user`, `/events` includes
  `turn_complete`, and `/session` lists numbered sessions. The same run
  confirmed `initialize` returned six sources, grouped context sources as
  `agents:project`, `agents:user`, `skills:project`, and `skills:user`, and
  migrated the SQLite database to `user_version = 3` with zero foreign-key
  errors.
- Host Chromium/Playwright was not available for an actual headless browser run;
  dashboard HTML was checked directly and contained `dashboard-sources`,
  `dashboard-bootstrap`, AGENTS.md, SKILL.md, and `system/skills`.
