# Test Report: settings-version-wizard
## Timestamp
2026-05-02 19:48:49 Asia/Seoul
## Environment
- Branch: `codex/settings-version-wizard`
- Package version: `0.1.7`
- Registry: `https://verdaccio.neurondev.net/`
- Installed prefix: `/tmp/ndx-install-verify-017`
- Runtime fixture: `/tmp/ndx-settings-version-verify-017`
## Executed Steps
1. Added settings version normalization and wizard repair coverage.
2. Updated package version, docs, test plan, and report.
3. Ran `yarn build && node --test dist/tests/config.test.js dist/tests/settings-wizard.test.js dist/tests/session-server.test.js`.
4. Ran `yarn test`.
5. Ran `npm run deploy`.
6. Published `@neurondev/ndx@0.1.7` to Verdaccio.
7. Installed `@neurondev/ndx@0.1.7` from Verdaccio into `/tmp/ndx-install-verify-017`.
8. Verified installed `ndx --version` and `ndxserver --version`.
9. Started installed `ndxserver --mock` against stale global and missing-version project settings.
10. Verified startup upgraded both settings files to `0.1.7`.
11. Reset project settings to `0.0.2`, posted dashboard `POST /api/reload`, and verified the project settings version returned to `0.1.7`.
## Observed Logs
- Focused tests: `21` tests passed, `0` failed.
- `yarn test`: `61` tests passed, `0` failed.
- `npm run deploy`: build and full tests passed; compose removed prior containers, rebuilt `hika00/ndx-sandbox:0.1.0`, started `ndx-ndx-sandbox-1`, wrote the sandbox verification file, and removed the compose container/network.
- `npm publish`: published `@neurondev/ndx@0.1.7`; shasum `44b3f3304985ba0ed60b008288cd92ea420c8a2a`.
- Installed binaries:
  - `ndx --version`: `0.1.7`
  - `ndxserver --version`: `0.1.7`
- Installed server:
  - `[session-server] ws://127.0.0.1:45442`
  - `[dashboard] http://127.0.0.1:45443`
  - Startup settings versions: `global=0.1.7 project=0.1.7`
  - Reload result: `ok=true project=0.1.7`
## Failures
One full `yarn test` run failed once in the pre-existing tool orchestration timing assertion after the code changed from `0.1.6` to `0.1.7`. The failing test passed when run alone, and the full `yarn test` plus `npm run deploy` reruns both passed.
## Reproducibility
Run the plan from `/mnt/f/dev/ndx` on branch `codex/settings-version-wizard`. Use an isolated npm prefix and a temporary `HOME` so installed-server verification does not mutate the developer's real `/home/.ndx`.
## Locator Outcome
No dashboard markup changed. Reload verification used the existing dashboard HTTP API. Existing locator contract remains covered by session-server tests and the dashboard browser report: `main[aria-labelledby="dashboard-title"][data-testid="ndx-dashboard"]`, `role="status"`, `dashboard-sources`, and `dashboard-bootstrap`.
