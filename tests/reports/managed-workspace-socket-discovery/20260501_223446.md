# Test Report: managed-workspace-socket-discovery

## Timestamp

2026-05-01 22:34:46 Asia/Seoul

## Environment

- Repository: `/mnt/f/dev/ndx`
- Shell: bash
- Docker: `Docker version 29.3.1, build c2be9cc`
- Docker Compose: `Docker Compose version v5.1.0`

## Executed Steps

1. Ran `yarn install --immutable`.
2. Ran `yarn build`.
3. Ran `node --test dist/tests/cli-workspace.test.js`.
4. Ran `yarn test`.
5. Checked Docker availability with `docker --version` and `docker compose version`.

## Observed Logs

- `yarn install --immutable`: completed with Yarn PnP ESM loader warning only.
- `yarn build`: passed.
- `node --test dist/tests/cli-workspace.test.js`: 2 tests passed, 0 failed.
- `yarn test`: 54 tests passed, 0 failed.
- `docker --version`: Docker 29.3.1.
- `docker compose version`: Docker Compose v5.1.0.

## Failures

- `npm run deploy` was not run because this deploy path clones the current
  pushed Git branch from GitHub. The socket-discovery fix was local and
  unpushed at verification time, so running deploy would not verify this change.

## Reproducibility

Run from `/mnt/f/dev/ndx`:

```bash
yarn install --immutable
yarn build
node --test dist/tests/cli-workspace.test.js
yarn test
```

## Locator Outcome

Not applicable; this behavior has no browser surface.
