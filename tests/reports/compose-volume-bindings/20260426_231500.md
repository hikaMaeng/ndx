# Test Report: compose-volume-bindings

## Timestamp

2026-04-26 23:15:00 KST

## Environment

- OS: Ubuntu 24.04 on WSL2
- Node: 22.22.2
- Docker: 29.3.1
- Compose: v5.1.0
- Git branch: `codex/compose-volume-bindings`
- Verified commit: `ae63e68`

## Executed Steps

1. Ran `docker compose config`.
2. Ran `npm test` locally.
3. Committed `ae63e68 chore: add compose runtime volume bindings`.
4. Pushed `codex/compose-volume-bindings` to `origin`.
5. Ran `npm run deploy`.
6. Confirmed `docker/volume/workspace/tmp/ndx-docker-verify.txt` exists on the host and contains `verified`.
7. Ran `NDX_GIT_REF=codex/compose-volume-bindings docker compose up -d --build ndx-agent`.
8. Ran `docker compose exec -T ndx-agent sh -lc 'pwd && test -d /opt/ndx && test -d /workspace && test -d /home/.ndx && ndx --help'`.

## Observed Logs

- `docker compose config` showed only the `NDX_GIT_REF` build arg and bind mounts from `./docker/volume/workspace` to `/workspace` and `./docker/volume/home-ndx` to `/home/.ndx`.
- Local `npm test`: TypeScript build succeeded; Node TAP reported 4 passing tests, 0 failures.
- Docker build printed `Building ndx from https://github.com/hikaMaeng/ndx.git@codex/compose-volume-bindings`.
- Dockerfile clone step cloned the feature branch into `/opt/ndx`.
- In-container `npm test` ran from `/opt/ndx` and reported 4 passing tests, 0 failures.
- In-container mock agent ran from `/workspace` and wrote `verified` to `tmp/ndx-docker-verify.txt`.
- Host file `docker/volume/workspace/tmp/ndx-docker-verify.txt` contained `verified`, proving the workspace bind mount is active.
- Runtime check printed `pwd=/workspace`, `opt_ok`, `workspace_ok`, `home_ndx_ok`, and `ndx_ok`.
- `ndx --help` printed the settings contract and interactive mode help from `/usr/local/bin/ndx`.

## Failures

None.

## Reproducibility

After pushing the branch, run:

```bash
npm run deploy
NDX_GIT_REF=codex/compose-volume-bindings docker compose up -d --build ndx-agent
docker compose exec -T ndx-agent sh -lc 'pwd && test -d /opt/ndx && test -d /workspace && test -d /home/.ndx && ndx --help'
cat docker/volume/workspace/tmp/ndx-docker-verify.txt
```

## Locator Outcome

Not applicable. This package has no browser UI.
