# Test Report: docker-startup-provenance
## Timestamp
2026-04-29 15:34:05 +0900

## Environment
- OS shell: bash
- Docker Compose service: `ndx-agent`
- Branch: `codex/process-manager-library`

## Executed Steps
1. Ran `npm test`.
2. Committed and pushed `Print Docker image provenance on startup`.
3. Rebuilt `ndx-agent:local` with `NDX_GIT_REF=codex/process-manager-library docker compose build --no-cache ndx-agent`.
4. Started `docker compose up -d ndx-agent`.
5. Observed no startup log lines because `docker-compose.yml` still overrode the image command with `sleep infinity`.
6. Removed the compose `command` override so the image default command can print provenance and then sleep.
7. Rebuilt and restarted compose from pushed commit `5a5000322c306f938ab841ab29a10f15debc2274`.
8. Observed provenance log lines but found `git log` opened a pager under `tty: true`, preventing the command from reaching `sleep infinity`.
9. Updated the startup command to use `git --no-pager log`.
10. Rebuilt and restarted compose from pushed commit `195b8a3b5217f7fbe66d18b325384af9dbb7abf8`.
11. Captured `docker compose logs --tail 12 ndx-agent`, `docker compose ps`, and `docker top ndx-ndx-agent-1`.

## Observed Logs
- `npm test` TAP summary: `tests 28`, `pass 28`, `fail 0`.
- Rebuilt image cloned commit `922d52fb200d4f39fde3b4cd88e21f9fccceb4c1`.
- Container inspection before compose fix showed `Config.Cmd=["sleep","infinity"]`, confirming the compose command override.
- Logs after compose fix included package, remote, ref, commit, branch, commit date, and subject.
- Process inspection showed `git log` waiting on `/usr/bin/pager`; startup command now disables the pager.
- Final startup logs:
  - `[ndx-image] package=ndx@0.1.0`
  - `[ndx-image] git_remote=https://github.com/hikaMaeng/ndx.git`
  - `[ndx-image] git_ref=codex/process-manager-library`
  - `[ndx-image] git_commit=195b8a3b5217f7fbe66d18b325384af9dbb7abf8`
  - `[ndx-image] git_branch=codex/process-manager-library`
  - `[ndx-image] git_commit_date=2026-04-29T15:39:18+09:00`
  - `[ndx-image] git_subject=Disable git pager in startup provenance`
  - `[ndx-image] node=v22.22.2 pnpm=10.33.0`
- Final compose state: `ndx-ndx-agent-1` is `Up`.
- Final container process: `sleep infinity`.

## Failures
First compose start produced no provenance logs because compose overrode the image command. The compose override was removed. Second compose start printed provenance but stopped at the Git pager; `git --no-pager log` fixed that startup hang. Final rebuild and compose start passed.

## Reproducibility
Run `docker compose logs --tail 40 ndx-agent` after rebuilding and starting the service from the pushed branch.

## Locator Outcome
No browser UI exists for this package. Browser locator verification is not applicable.
