# Test Report: docker-startup-provenance
## Timestamp
2026-04-29 15:34:05 +0900

## Environment
- OS shell: bash
- Docker Compose service: `ndx-agent`
- Branch: `codex/process-manager-library`

## Executed Steps
1. Ran `npm test`.
2. Committed and pushed `Print Docker image provenance on startup`.
3. Rebuilt `ndx-agent:local` with `NDX_GIT_REF=codex/process-manager-library docker compose build --no-cache ndx-agent`.
4. Started `docker compose up -d ndx-agent`.
5. Observed no startup log lines because `docker-compose.yml` still overrode the image command with `sleep infinity`.
6. Removed the compose `command` override so the image default command can print provenance and then sleep.

## Observed Logs
- `npm test` TAP summary: `tests 28`, `pass 28`, `fail 0`.
- Rebuilt image cloned commit `922d52fb200d4f39fde3b4cd88e21f9fccceb4c1`.
- Container inspection before compose fix showed `Config.Cmd=["sleep","infinity"]`, confirming the compose command override.

## Failures
First compose start produced no provenance logs because compose overrode the image command. The compose override was removed.

## Reproducibility
Run `docker compose logs --tail 40 ndx-agent` after rebuilding and starting the service from the pushed branch.

## Locator Outcome
No browser UI exists for this package. Browser locator verification is not applicable.
