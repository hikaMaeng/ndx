# Test Report: model-pools

## Timestamp

2026-04-30T12:34:05+09:00

## Environment

- Workspace: `/mnt/f/dev/ndx`
- Shell: bash
- Commands: `npm run build`, `npm test`, `npx prettier --write ...`,
  `npm run deploy`

## Executed Steps

1. Built TypeScript with `npm run build`.
2. Ran `npm test`.
3. Verified legacy string model settings normalize to a single session pool.
4. Verified object model pools parse `session`, `worker`, and `reviewer`.
5. Verified `SessionServer` assigns new sessions from `model.session` in
   round-robin order.
6. Formatted changed files with Prettier.
7. Re-ran `npm test`.
8. Ran `npm run deploy` after pushing `codex/fixmodelsetting` so Docker could
   clone the current branch.
9. Stopped the deploy run after the in-container test process stopped producing
   output after subtest 34, then removed the run container and compose network.

## Observed Logs

- `npm run build` completed with `tsc -p tsconfig.json`.
- First `npm test` run had one transient failure in `agent abort signal propagates
  to external tool processes`.
- `node --test dist/tests/agent.test.js` passed immediately afterward.
- Prettier completed for changed source, test, and docs files.
- Final `npm test` result after formatting: `# tests 43`, `# pass 43`,
  `# fail 0`, `# duration_ms 19788.101306`.
- `npm run deploy` completed host build, compose down, no-cache image build,
  GitHub clone of `codex/fixmodelsetting`, image export, and started
  in-container `npm test`.
- In-container `npm test` reached `ok 34 - session ownership uses last prompt
  attempt across socket servers` and then remained active without output.
- Cleanup removed `ndx-ndx-agent-run-f78b5752e018` and `ndx_default`.

## Failures

Host build and test had no failures. `npm run deploy` did not complete because
the in-container `npm test` hung after subtest 34 and was manually stopped before
the final mock CLI verification.

## Reproducibility

Run `npm test` from `/mnt/f/dev/ndx`. To reproduce the deploy result, run
`npm run deploy` from branch `codex/fixmodelsetting`.

## Locator Outcome

No browser UI exists for this package.
