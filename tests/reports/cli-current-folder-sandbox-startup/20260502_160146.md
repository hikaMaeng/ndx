# Test Report: cli-current-folder-sandbox-startup

## Timestamp

2026-05-02T16:01:46+09:00

## Environment

- Repository: `/mnt/f/dev/ndx`
- Shell: bash
- Runtime: Node.js 22, Yarn PnP
- Docker: local Docker engine through `docker` and `docker compose`
- Browser: Playwright CLI 1.59.1 with Chromium runtime installed in user cache

## Executed Steps

1. Preserved pre-existing local changes on `main` with commit `21fe3e7`.
2. Created and switched to branch `core/fixndx`.
3. Updated CLI startup so the current folder is the project folder and startup no longer asks for a workspace folder or project selection.
4. Updated first-run settings wizard to write `/home/.ndx/settings.json` instead of project `.ndx/settings.json`.
5. Moved bootstrapped core capability packages from `/home/.ndx/system/core/tools` to `/home/.ndx/system/tools`.
6. Updated server method exposure to omit `project/list` and `project/create`.
7. Updated Docker sandbox management to label containers by resolved physical project folder, reuse the labeled container for the same folder, and add a deterministic suffix only for same-basename path collisions.
8. Updated docs and test plan coverage for CLI/server responsibility, image-level sandbox capabilities, current-folder startup, and per-physical-folder Docker sandbox ownership.
9. Ran targeted build and tests:
   `yarn build && node --test dist/tests/cli-workspace.test.js dist/tests/config.test.js dist/tests/tools.test.js dist/tests/session-server.test.js dist/tests/cli-session-client.test.js dist/tests/settings-wizard.test.js`.
10. Ran full tests: `yarn test`.
11. Ran Docker physical-folder sandbox verification with two different temporary folders sharing basename `same`.
12. Ran final deploy path: `npm run deploy`.
13. Started `node dist/src/cli/main.js serve --mock --listen 127.0.0.1:45231 --dashboard-listen 127.0.0.1:45232`.
14. Ran dashboard browser verification:
    `playwright screenshot --browser chromium --viewport-size 1280,720 --wait-for-selector 'main[data-testid="agent-dashboard-placeholder"]' http://127.0.0.1:45232/dashboard /tmp/ndx-dashboard-verification.png`.
15. Fetched `/dashboard` and asserted the title, `main` landmark/test id, heading, and `role="status"` contract.

## Observed Logs

- Targeted Node tests: `35` tests, `35` pass, `0` fail.
- Full `yarn test`: `56` tests, `56` pass, `0` fail.
- Docker physical-folder verification:
  - first folder container: `ndx-tool-same`
  - second same-basename folder container: `ndx-tool-same-9f244af4d1ac`
  - repeated first folder request reused `ndx-tool-same`
  - labels recorded each resolved physical project path.
- `npm run deploy`: build passed, `yarn test` passed, `docker compose down --remove-orphans` ran, `docker compose build --no-cache ndx-sandbox` built `hika00/ndx-sandbox:0.1.0`, compose started `ndx-sandbox`, wrote `/workspace/tmp/ndx-docker-verify.txt`, then removed the compose container and network.
- Browser verification checks:
  - `title`: true
  - `main`: true
  - `heading`: true
  - `status`: true

## Failures

- First Playwright screenshot attempt failed because the Chromium runtime was not installed in the user cache. `playwright install chromium` installed the browser runtime without changing repository dependencies, and the verification then passed.

## Reproducibility

Run from `/mnt/f/dev/ndx`:

```bash
yarn test
npm run deploy
node dist/src/cli/main.js serve --mock --listen 127.0.0.1:45231 --dashboard-listen 127.0.0.1:45232
playwright screenshot --browser chromium --viewport-size 1280,720 --wait-for-selector 'main[data-testid="agent-dashboard-placeholder"]' http://127.0.0.1:45232/dashboard /tmp/ndx-dashboard-verification.png
```

For physical-folder Docker sandbox reuse, call `ensureDockerSandbox` twice with the same resolved folder and once with a different folder that shares the same basename, then inspect `dev.ndx.workspace` labels.

## Locator Outcome

- Browser locator strategy: stable test id selector `main[data-testid="agent-dashboard-placeholder"]`.
- Semantic contract confirmed by HTML assertions for `aria-labelledby="dashboard-title"`, visible `h1` text `ndx Agent Service`, and `role="status"`.
- No ambiguity or flakiness observed after Chromium runtime installation.
