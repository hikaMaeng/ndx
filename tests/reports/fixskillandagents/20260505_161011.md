# Test Report: fixskillandagents

## Environment

- Branch: `fixskillandagents`
- Package version: `0.1.30`
- Runtime: Node.js with Yarn 4 Plug'n'Play
- Registry: `https://verdaccio.neurondev.net/`

## Commands

1. `yarn install --immutable`
2. `yarn build`
3. `node --test dist/tests/config.test.js --test-name-pattern "AGENTS and skills"`
4. `node --test dist/tests/runtime.test.js --test-name-pattern "instruction sources|skill tool"`
5. `node --test dist/tests/session-server.test.js --test-name-pattern "websocket reset|skill tool events|lite mode|compact stores"`
6. `yarn test`
7. `npm run deploy`
8. Docker Playwright dashboard check against source server
   `http://host.docker.internal:46524`
9. `npm view @neurondev/ndx versions --registry https://verdaccio.neurondev.net/ --json`
10. `npm pack --dry-run --registry https://verdaccio.neurondev.net/`
11. `npm publish --registry https://verdaccio.neurondev.net/`
12. `npm install --prefix /tmp/ndx-install-verify-030 @neurondev/ndx@0.1.30 --registry https://verdaccio.neurondev.net/`
13. Installed `ndx --version` and `ndxserver --version`
14. Installed `ndxserver serve --mock` on `127.0.0.1:46623` and dashboard on `127.0.0.1:46624`
15. Installed `ndx --connect ws://127.0.0.1:46623 --cwd /tmp/ndx-project-030 "list files"`
16. Installed SessionClient checks for `/context`, `/lite on`, and `/compact`
17. Docker Playwright dashboard source check against
    `http://host.docker.internal:46624`
18. Installed SessionClient close/reset verification that the server remains
    reachable after client disconnect.

## Results

- Targeted config test passed and verified AGENTS.md cascade plus skill scan
  order, including exclusion of `skills/.system`.
- Targeted runtime tests passed and verified instruction-source context
  grouping plus skill tool-call exclusion from saved model context.
- Session server tests passed and verified skill tool events remain audited in
  SQLite while excluded from `iscontext` replay, plus WebSocket client resets no
  longer crash the server.
- Full `yarn test` passed: 86 tests passed.
- Final `npm run deploy` passed: build, full tests, compose down, no-cache
  sandbox image build, compose up, sandbox write check, and compose down all
  completed.
- Headless browser dashboard verification passed with role/test-id locators:
  `main[data-testid="ndx-dashboard"]`, Dashboard views navigation, `Overview`,
  `Session Logs`, `Users`, `Reload`, `role="status"`,
  `dashboard-sources`, and `dashboard-bootstrap`.
- Verdaccio publish succeeded for `@neurondev/ndx@0.1.30`.
- Installed package verification passed: `ndx --version` and
  `ndxserver --version` both returned `0.1.30`.
- Installed server startup loaded six expected sources:
  global settings, three AGENTS.md files, project skill, and user system skill.
- Installed `/context` output separated `agents_project`, `agents_user`,
  `skills_project`, and `skills_user`.
- Installed `/lite on` and `/compact` on a fresh session returned the expected
  unchanged messages because no saved turns existed yet.
- Installed server stayed reachable after SessionClient close/reset; the
  earlier `ECONNRESET` crash was fixed by server-side socket error handling.

## Notes

- Docker headless browser access to the host dashboard required
  `host.docker.internal`; `127.0.0.1` from the container did not reach the WSL
  host listener.
- Node emitted the expected experimental `node:sqlite` warning during session
  server tests and installed server startup.
