# Test Report: client-context-model-routing
## Timestamp
2026-04-30 16:32:58 +0900

## Environment
- Workspace: `/mnt/f/dev/ndx`
- Branch: `codex/clientcontext`
- Browser verification: not applicable; this package has no frontend view.

## Executed Steps
1. Ran `npm run build`.
2. Ran `npm test`.
3. Updated docs and this verification record after the local test run.
4. Re-ran `npm test` after docs and report updates.
5. Removed the `previousResponseId` compatibility parameter from the shared
   model client contract and re-ran `npm test`.
6. Pushed `codex/clientcontext` and ran `npm run deploy`.

## Observed Logs
- `npm run build`: passed.
- First `npm test`: failed one new assertion because the test captured a mutable
  input array reference.
- Fixed the test to store JSON snapshots of model inputs.
- Second `npm test`: passed 47/47 tests.
- Final `npm test`: passed 47/47 tests.
- Post-interface-cleanup `npm test`: passed 47/47 tests.
- Branch `npm run deploy`: passed. Docker build cloned
  `https://github.com/hikaMaeng/ndx.git@codex/clientcontext`, in-container
  `npm test` passed 47/47 tests, mock agent wrote
  `tmp/ndx-docker-verify.txt`, and compose removed the network at cleanup.

## Failures
- Initial local test failure was test-only. The expected second model request
  was mutated after final assistant text was appended.

## Reproducibility
Run:

```bash
npm test
```

The test suite covers local context stack replay, OpenAI Responses without
`previous_response_id`, per-request round-robin routing, `@deep` custom pool
routing, and session-server base-config behavior.

## Locator Outcome
Not applicable. No browser UI exists in this package.
