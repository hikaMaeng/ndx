# Test Report: tool-orchestration

## Timestamp

2026-04-29 02:36:20 KST

## Environment

- Workspace: `/mnt/f/dev/ndx`
- Shell: bash
- Local command: `npm test`
- Container command: `npm run deploy`

## Executed Steps

1. Added `tests/tool-orchestration.test.ts`.
2. Added `tests/plans/tool-orchestration-20260429.md`.
3. Ran `npm test`.
4. Fixed the model-driven tool coverage assertion from strict registry order to
   set equality, because the scripted model intentionally calls scheduling probe
   tools before the rest of the registry.
5. Re-ran `npm test`.
6. Ran `npm run deploy`.

## Observed Logs

- First `npm test`: failed 1 of 14 tests because the tool-call order assertion
  expected registry order.
- Final local `npm test`: `# tests 14`, `# pass 14`, `# fail 0`.
- New model-driven test used every configured tool exposed by the fixture
  registry:
  `update_plan`, `request_user_input`, `spawn_agent`, `send_input`,
  `send_message`, `followup_task`, `resume_agent`, `wait_agent`,
  `close_agent`, `list_agents`, `spawn_agents_on_csv`,
  `report_agent_job_result`, `direct_echo`, `parallel_a`, `parallel_b`,
  `serial_first`, `serial_second`, `mcp__probe__mcp_echo`.
- Parallel scheduling check passed: `parallel_a` and `parallel_b` overlapped in
  timestamp ranges.
- Serial scheduling check passed: `serial_first` started after both parallel
  tools ended, and `serial_second` started after `serial_first` ended.
- Process isolation check passed: parallel external calls recorded distinct
  parent process ids, proving separate worker processes were used.
- Direct tool execution check passed for every configured tool through
  `registry.execute`, without using the agent loop.
- `npm run deploy`: local build succeeded, Docker Compose refreshed the stack,
  `ndx-agent:local` rebuilt, container tests passed with `# tests 11`,
  mock CLI verification returned `mock agent completed`, and Compose cleanup
  removed the network.

## Failures

- Initial failure: the new integration test asserted exact registry order for
  model-emitted tool calls.
- Resolution: changed the coverage assertion to sorted set equality and left the
  ordering-sensitive checks focused on the explicit parallel/serial probe tools.

## Reproducibility

- Run `npm test` from `/mnt/f/dev/ndx` to execute the full local suite,
  including the new orchestration tests.
- Run `npm run deploy` from `/mnt/f/dev/ndx` to verify build, Docker Compose,
  container tests, and mock CLI execution.
- Note: `npm run deploy` clones the current GitHub branch inside the Docker
  image. Local unpushed test additions are verified by local `npm test`; deploy
  verifies the repository's remote container path.

## Locator Outcome

Not applicable. This change has no browser-rendered UI.
