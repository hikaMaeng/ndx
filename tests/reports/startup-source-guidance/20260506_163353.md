# Startup Source Guidance - 2026-05-06 16:33:53

## Change Under Test

- Startup source guidance now tells the model that AGENTS.md and skill catalog
  entries are already loaded.
- Skill-driven requests now explicitly instruct the model to call `load_skill`
  before filesystem or shell exploration.

## Expected Result

- Models should spend fewer early turns trying to `type`, `cat`, or shell-open
  Windows startup source paths from the Linux tool sandbox.
- `request_user_input` remains absent from model-facing tool schemas.
- `load_skill` remains available and can load host `SKILL.md` content.

## Results

- `yarn exec tsc -p tsconfig.json --noEmit`: passed.
- `yarn exec prettier --check ...`: passed.
- Targeted config, skill-loading, and internal tool registry tests: passed.
- `yarn install --immutable`: passed with the existing Yarn PnP ESM warning.
- `yarn test`: passed 88 tests.
- `npm run deploy`: passed build, full test suite, Docker compose
  down/build-no-cache/up verification, and compose teardown.
- `npm publish --registry https://verdaccio.neurondev.net/`: published
  `@neurondev/ndx@0.1.35`.
- Installed package verification for `@neurondev/ndx@0.1.35`: passed.
  Installed CLI versions returned `0.1.35`; installed tool schemas include
  `list_skills` and `load_skill`, exclude `request_user_input`, and installed
  config instructions include the stronger `load_skill`-first and no-shell
  startup source guidance.
- Windows global install verification in `F:\dev\test2`: passed.
  `ndxserver` restarted from the Windows global npm package, reported
  `ndx-ts-session-server 0.1.35`, printed `[sources] 9`, and a skill-focused
  `ndx --connect` request used `load_skill` as the first tool call without
  `type`, `cat`, or PowerShell path probing.
