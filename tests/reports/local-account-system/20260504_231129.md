# Test Report: local-account-system
## Timestamp
2026-05-04 23:11:29 Asia/Seoul
## Environment
- Workspace: `/mnt/c/Users/hika0/.codex/worktrees/e391/ndx`
- Branch: `localaccount`
- Shell: bash
- Package manager: Yarn 4.14.1
## Executed Steps
1. Ran `yarn install --immutable` after aligning `yarn.lock` bin metadata with
   `package.json`.
2. Ran `yarn build`.
3. Ran `node --test dist/tests/session-server.test.js dist/tests/cli-session-client.test.js`.
4. Ran `yarn test`.
5. Ran `npm run deploy`.
6. Started `node dist/src/cli/main.js serve --mock --listen 127.0.0.1:45123 --dashboard-listen 127.0.0.1:45124`.
7. Ran Playwright Chromium from `hika00/ndx-sandbox:0.1.1` against
   `http://host.docker.internal:45124/dashboard`.
8. Re-ran `yarn build` and targeted account/session tests after removing unused
   password/delete compatibility store methods.
## Observed Logs
- `yarn install --immutable`: completed with Yarn PnP ESM loader warning only.
- `yarn build`: exited `0`.
- Targeted Node tests: 22 tests passed, 0 failed.
- Full `yarn test`: 76 tests passed, 0 failed.
- `npm run deploy`: build and full tests passed; Docker Compose removed prior
  resources, rebuilt `hika00/ndx-sandbox:0.1.1` with `--no-cache`, started
  `ndx-sandbox`, wrote `/workspace/tmp/ndx-docker-verify.txt`, then removed the
  container and network.
- Browser locator counts:
  `{"main":1,"nav":1,"reload":1,"exit":1,"status":2,"sources":1,"bootstrap":1}`.
- Final targeted Node tests after cleanup: 22 tests passed, 0 failed.
- Node emitted the expected experimental `node:sqlite` warning during server
  tests.
## Failures
- Initial immutable install failed because `yarn.lock` still mapped `ndxserver`
  to `dist/src/cli/main.js` while `package.json` maps it to
  `dist/src/cli/ndxserver.js`; lock metadata was corrected.
- Initial CLI test build failed because tests still constructed the removed
  password login shape; tests were updated to local account login.
## Reproducibility
Run:

```bash
yarn install --immutable
yarn build
node --test dist/tests/session-server.test.js dist/tests/cli-session-client.test.js
yarn test
npm run deploy
```
## Locator Outcome
Browser verification passed from the deployed sandbox image using Playwright
Chromium. The working locator strategy was:
`main[aria-labelledby="dashboard-title"][data-testid="ndx-dashboard"]`,
`getByRole("navigation", { name: "Server actions" })`, buttons named `Reload`
and `Exit`, status/alert role output, and `dashboard-sources` plus
`dashboard-bootstrap` test ids. No ambiguity was observed.
