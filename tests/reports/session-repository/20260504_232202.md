# Test Report: session-repository
## Timestamp
2026-05-04T23:22:02+0900

## Environment
- Workspace: `/mnt/c/Users/hika0/.codex/worktrees/b2f2/ndx`
- Runtime: Node.js with experimental `node:sqlite`
- Package manager: Yarn 4.14.1

## Executed Steps
1. `yarn install --immutable`
2. `yarn install`
3. `yarn build`
4. `node --test dist/tests/session-server.test.js --test-name-pattern "SQLite persistence|project identity|dashboard session logs|ownership is tracked|ownership uses last prompt|ownership discards|lite mode"`
5. `npm run deploy`
6. Browser locator check from the rebuilt sandbox image against
   `http://host.docker.internal:45624/dashboard`
7. Repeated the browser locator check after the final clean-schema changes at
   `2026-05-05T00:29:20+0900`.
8. Merged `origin/main` into `fixsessionrepository`, resolved local-account
   conflicts, and reran `yarn build`, `yarn test`, `npm run deploy`, and browser
   locator verification at `2026-05-05T01:18:33+0900`.

## Observed Logs
- `yarn install --immutable` failed before code verification because
  `yarn.lock` still mapped `ndxserver` to `dist/src/cli/main.js` while
  `package.json` maps it to `dist/src/cli/ndxserver.js`.
- `yarn install` completed and updated the lockfile with Yarn's ESM PnP loader
  warning.
- `yarn build` passed.
- Focused session-server run passed 15 tests.
- After the clean-schema reset, focused session-server coverage passed 15 tests
  for persistence, project identity, restore, lite, compact, and ownership.
- `npm run deploy` completed build, full test suite, Docker compose down,
  no-cache sandbox build, compose up, sandbox command verification, and compose
  down cleanup.
- Browser locator verification passed against the dashboard using documented
  selectors, and the final rerun returned
  `{"ok":true,"status":"Dashboard is running."}`.
- After merging `origin/main`, `yarn test` passed 77 tests and `npm run deploy`
  repeated the 77-test run plus Docker compose refresh successfully.
- Node emitted the expected experimental `node:sqlite` warning.

## Failures
- Initial focused test run exposed two implementation/test gaps:
  `session.ownerid` is now intentionally set to the client id at first claim,
  and project path uniqueness prevented path reuse after deleting `.ndx/.project`.
  Both were fixed before the passing focused run.
- During clean-schema conversion, context replay initially read stored wrapper
  records instead of runtime events; `sessiondata.iscontext` replay now maps
  payloads back to runtime events before model history reconstruction.

## Reproducibility
Run the focused verification after `yarn build`:

```bash
node --test dist/tests/session-server.test.js --test-name-pattern "SQLite persistence|project identity|dashboard session logs|ownership is tracked|ownership uses last prompt|ownership discards|lite mode"
```

Full `yarn test` passed through `npm run deploy`.

## Locator Outcome
The dashboard check passed with:

- `main[aria-labelledby=dashboard-title][data-testid=ndx-dashboard]`
- `nav` named `Server actions`
- buttons named `Reload` and `Exit`
- `data-testid="dashboard-session-logs"`
- `data-testid="session-log-table"`
- label `Project`
