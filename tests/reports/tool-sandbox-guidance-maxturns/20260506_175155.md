# Tool Sandbox Guidance And Max Turns - 2026-05-06 17:51:55

## Change Under Test

- Agent max-turn exhaustion should emit a warning and complete the current turn
  without closing the session.
- Model-facing operational and tool descriptions should explain that
  Docker-backed tools run in Linux with the project at `/workspace`.

## Expected Result

- `agent stopped after max_turns=...` appears as a warning, not a session-ending
  error.
- Models should avoid Windows `dir /b`, PowerShell, drive-letter shell paths,
  and mixed paths such as `/workspace/workspace`.
- `load_skill` remains an internal TypeScript runtime tool and ordinary
  workspace tools remain Docker-backed external tools.

## Results

- Passed `yarn exec tsc -p tsconfig.json --noEmit`.
- Passed `yarn exec prettier --check` for the changed source, test, doc, plan,
  and report files after formatting `tests/agent.test.ts`.
- Passed targeted tests:
  `node --test dist/tests/agent.test.js --test-name-pattern "max turns|skill"`,
  `node --test dist/tests/session-server.test.js --test-name-pattern "max-turn"`,
  and
  `node --test dist/tests/tools.test.js --test-name-pattern "bootstrapped core"`.
- Passed `yarn install --immutable`.
- Passed full `yarn test`: 89 tests passed.
- Passed `npm run deploy`: build, full test suite, sandbox image build, compose
  refresh, container start/stop, and cleanup completed successfully.
- Published `@neurondev/ndx@0.1.36` to
  `https://verdaccio.neurondev.net/`.
- Installed `@neurondev/ndx@0.1.36` into `/tmp/ndx-install-verify-036` and
  verified both `ndx --version` and `ndxserver --version` returned `0.1.36`.
- Verified the installed package exposes `load_skill` and `list_skills` through
  the internal skill tool module, does not expose `request_user_input` there,
  and includes `/workspace`, POSIX bash, and `/workspace/workspace` guidance in
  core tool metadata.
- Installed `@neurondev/ndx@0.1.36` globally on Windows, then verified
  `ndx --version` and `ndxserver --version` both returned `0.1.36`.
- Restarted the Windows server from `F:\dev\test2`; it reported
  `ndx-ts-session-server 0.1.36`, `host-process v25.9.0 (win32/x64)`, and
  Docker tool sandbox `hika00/ndx-sandbox:0.1.1 (/workspace, /home/.ndx)`.
- Ran a Windows `F:\dev\test2` non-interactive scenario requesting
  `web-service-scaffold`; the first model tool call was `load_skill`, and the
  final answer was `Web Service Scaffold`.
- Ran a CLI mock scenario with project `maxTurns: 1`; the turn printed
  `[warning] agent stopped after max_turns=1; current turn ended without closing the session`
  and exited with code 0.
