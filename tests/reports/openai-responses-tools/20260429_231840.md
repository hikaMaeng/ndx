# Test Report: openai-responses-tools

## Timestamp

2026-04-29 23:18:40 KST

## Environment

- Workspace: `/mnt/f/dev/ndx`
- Branch: `codex/sessionlistrestore`
- Shell: bash

## Executed Steps

1. Reproduced the visible failure from Docker Compose prompt output:
   OpenAI-compatible Responses rejected `tools.0.type`.
2. Inspected container `/home/.ndx/core/tools/*/tool.json` and confirmed core
   manifests are Chat Completions-compatible function schemas.
3. Updated the Responses adapter to convert nested function tool schemas into
   the flat Responses function tool shape before sending.
4. Added adapter-level coverage for the conversion.

## Observed Logs

- `npm test`: passed, 36 tests.

## Failures

- Original failure: `OpenAI responses failed: 400`, `param: tools.0.type`,
  `code: invalid_string`.

## Reproducibility

Run:

```bash
npm test
NDX_GIT_REF=codex/sessionlistrestore docker compose up -d --build ndx-agent
```
