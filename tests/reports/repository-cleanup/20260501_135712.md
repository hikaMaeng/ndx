# Test Report: Repository Cleanup

## Timestamp

2026-05-01 13:57:12 +0900

## Environment

- Workspace: `/mnt/f/dev/ndx`
- Branch: `codex/cleanup-root-clutter`
- Node.js: `v22.22.2`
- Yarn: `4.14.1`
- Package mode: Plug'n'Play with `enableGlobalCache: true`
- Docker image: `ndx-agent:local`

## Executed Steps

1. Ran `yarn install --immutable`.
2. Ran `yarn build`.
3. Ran `npm run build`.
4. Ran `yarn test`.
5. Ran `npm test`.
6. Pushed branch `codex/cleanup-root-clutter` so Docker remote-clone deploy could build the current branch.
7. Ran `npm run deploy`.
8. Started `ndx-agent:local` as a containerized dashboard server and checked `http://127.0.0.1:45124/dashboard`.
9. Searched repository and global `.codex` instructions for removed package-manager and catalog terms.

## Observed Logs

- `yarn install --immutable`: completed with the Yarn PnP ESM loader warning only.
- `yarn build`: passed.
- `npm run build`: passed through `yarn exec tsc -p tsconfig.json`.
- `yarn test`: 50 tests, 50 passed.
- `npm test`: 50 tests, 50 passed.
- First deploy attempt built the image and passed in-container `yarn test`, then failed because the mock agent started in `/workspace` without a settings file.
- Deploy script was adjusted to start the mock agent in `/opt/ndx` and write `/workspace/tmp/ndx-docker-verify.txt`.
- Final `npm run deploy`: passed. Docker removed prior compose resources, rebuilt `ndx-agent:local` from the pushed branch, ran in-container `yarn test` with 50 passing tests, wrote `verified` to `/workspace/tmp/ndx-docker-verify.txt`, and removed the compose network.
- Package/corpus scan for the removed package-manager and catalog terms: no matches in active repo docs/configs/source/test records or global `.codex` AGENTS/skills.
- Workspace `node_modules`: absent.

## Failures

- Initial deploy failed at the mock-agent step because `/workspace` had no `.ndx/settings.json`.
- Fix: run the mock agent from `/opt/ndx` so config discovery uses the cloned project settings, while the task still writes to the `/workspace` bind mount.

## Reproducibility

```bash
yarn install --immutable
yarn test
npm test
npm run deploy
```

The deploy path requires the current branch to exist on `origin` because the
Dockerfile clones `https://github.com/hikaMaeng/ndx.git` at `NDX_GIT_REF`.

## Locator Outcome

Dashboard check target: deployed `ndx-agent:local` image, `GET /dashboard`.

- `main` landmark: found.
- Heading text `ndx Agent Service`: found.
- `role="status"`: found.
- `data-testid="agent-dashboard-placeholder"`: found.

No ambiguity or flakiness was observed in the placeholder markup.
