# Test Report: Main Docker Compose Verification

## Timestamp

2026-05-01 14:23:07 +0900

## Environment

- Workspace: `/tmp/ndx-main-yarn-pnp-verify`
- Branch: `main`
- Commit: `d9773de64eac79830802002d3a150fca3de1ca9c`
- Node.js: `v22.22.2`
- Yarn: `4.14.1`
- Package mode: Plug'n'Play with `enableGlobalCache: true`
- Docker image: `ndx-agent:local`

## Executed Steps

1. Fetched `origin/main` after PR #14 was merged.
2. Created a clean main worktree at `/tmp/ndx-main-yarn-pnp-verify`.
3. Ran `yarn install --immutable`.
4. Ran `yarn test`.
5. Ran `npm test`.
6. Ran `npm run deploy`.
7. Started the rebuilt `ndx-agent:local` image as a dashboard server.
8. Checked `http://127.0.0.1:45124/dashboard` for stable headless-browser locators.

## Observed Logs

- `yarn install --immutable`: passed with only the Yarn PnP ESM loader warning.
- `yarn test`: 50 tests, 50 passed.
- `npm test`: 50 tests, 50 passed.
- `npm run deploy`: passed. Docker removed prior compose resources, rebuilt `ndx-agent:local` from `https://github.com/hikaMaeng/ndx.git@main`, ran in-container `yarn test` with 50 passing tests, wrote `verified` to `/workspace/tmp/ndx-docker-verify.txt`, and removed the compose network.
- Dashboard container check: passed against the rebuilt `ndx-agent:local` image.

## Locator Outcome

Dashboard check target: `GET /dashboard` from the deployed image.

- Heading text `ndx Agent Service`: found.
- `role="status"`: found.
- `data-testid="agent-dashboard-placeholder"`: found.

## Failures

None.

## Reproducibility

```bash
git fetch origin main
git worktree add /tmp/ndx-main-yarn-pnp-verify main
cd /tmp/ndx-main-yarn-pnp-verify
yarn install --immutable
yarn test
npm test
npm run deploy
```
