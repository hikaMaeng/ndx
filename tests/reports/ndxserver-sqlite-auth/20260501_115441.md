# Test Report: ndxserver-sqlite-auth

## Timestamp

2026-05-01 11:54:41 +0900

## Environment

- Workspace: `/mnt/f/dev/ndx`
- Branch: `codex/ndxserver`
- Runtime: local Node.js test runner with experimental `node:sqlite`.

## Executed Steps

1. Ran `npm run build`.
2. Ran `node --test dist/tests/session-server.test.js dist/tests/cli-session-client.test.js`.
3. Ran `npm test`.
4. Ran `npm run deploy` after pushing `codex/ndxserver`.

## Observed Logs

- `npm run build`: passed.
- Targeted tests: 15 tests passed, 0 failed.
- Full test suite: 50 tests passed, 0 failed.
- Deploy: passed. Docker built `ndx-agent:local` from
  `https://github.com/hikaMaeng/ndx.git@codex/ndxserver`, ran 50 in-container
  tests with 0 failures, executed the mock agent in `/workspace`, wrote
  `tmp/ndx-docker-verify.txt`, and removed the compose network.
- SQLite emitted Node's experimental feature warning during session-server tests.

## Failures

- None in targeted local verification.
- PR, merge, and main re-verification are still pending in this report.

## Reproducibility

Run from the repository root:

```bash
npm run build
node --test dist/tests/session-server.test.js dist/tests/cli-session-client.test.js
npm test
npm run deploy
```

## Locator Outcome

The dashboard placeholder was verified by `session server exposes account methods, client identity, and dashboard placeholder` using the HTTP dashboard URL returned by `SessionServer.listen`. The response contained `data-testid="agent-dashboard-placeholder"` and `role="status"`.
