# Test Report: session lite user turn boundary

## Timestamp

2026-05-04 01:40:40 KST

## Environment

- Repository: `/mnt/f/dev/ndx`
- Shell: bash
- Node: repository test runtime
- Docker image: `hika00/ndx-sandbox:0.1.1`

## Executed Steps

1. Ran `yarn build`.
2. Ran `node --test dist/tests/session-server.test.js`.
3. Ran `yarn test`.
4. Ran `npm run deploy`.

## Observed Logs

- `yarn build`: passed.
- `node --test dist/tests/session-server.test.js`: 14 tests passed, 0 failed.
- `yarn test`: 71 tests passed, 0 failed.
- `npm run deploy`: passed; build and full tests passed, compose resources were
  removed, `ndx-sandbox` rebuilt with `--no-cache`, sandbox write verification
  completed, and compose resources were removed again.
- Lite-specific passing tests:
  - `session lite mode prunes previous tool logs when a new user turn starts`
  - `session lite mode prunes failed prior turn tool logs on next user request`

## Failures

- Direct `node --test tests/session-server.test.ts` failed before build because
  Node strip-only TypeScript mode does not support existing parameter
  properties. The repository-supported path is `yarn build` followed by tests
  from `dist/`.

## Reproducibility

Run from `/mnt/f/dev/ndx`:

```bash
yarn build
node --test dist/tests/session-server.test.js
yarn test
npm run deploy
```

## Locator Outcome

Not applicable; this change has no browser surface.
