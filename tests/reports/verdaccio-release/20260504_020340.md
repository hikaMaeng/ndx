# Test Report: verdaccio-release

## Timestamp

2026-05-04 02:03:40 KST

## Environment

- Repository: `/mnt/f/dev/ndx`
- Registry: `https://verdaccio.neurondev.net/`
- Package version: `0.1.15`
- Shell: bash
- Docker image: `hika00/ndx-sandbox:0.1.1`

## Executed Steps

1. Queried Verdaccio before publish:
   `npm view @neurondev/ndx version versions --registry https://verdaccio.neurondev.net/ --json`.
2. Confirmed Verdaccio auth:
   `npm whoami --registry https://verdaccio.neurondev.net/`.
3. Bumped local package/docs version from `0.1.14` to `0.1.15`.
4. Ran `npm run deploy`.
5. Verified local built binaries:
   `node dist/src/cli/main.js --version` and
   `node dist/src/cli/main.js --mock --version`.
6. Ran `npm pack --dry-run --registry https://verdaccio.neurondev.net/`.
7. Attempted `npm publish --registry https://verdaccio.neurondev.net/`.
8. Re-queried Verdaccio after the failed publish.
9. Checked registry reachability with `npm ping` and `curl -I`.
10. Re-ran `npm ping` after the registry recovered.
11. Re-ran `npm publish --registry https://verdaccio.neurondev.net/`.
12. Queried Verdaccio after the successful publish.
13. Installed `@neurondev/ndx@0.1.15` into `/tmp/ndx-install-verify-015`.
14. Verified installed `ndx --version` and `ndxserver --version`.

## Observed Logs

- Before publish, Verdaccio latest for `@neurondev/ndx` was `0.1.14`; versions
  included `0.1.0` through `0.1.14`.
- `npm whoami`: `codexmon5kiq2`.
- `npm run deploy`: passed; build and 71 tests passed, compose resources were
  removed, `ndx-sandbox` rebuilt with `--no-cache`, sandbox write verification
  ran, and compose resources were removed.
- Local binary versions:
  - `node dist/src/cli/main.js --version`: `0.1.15`
  - `node dist/src/cli/main.js --mock --version`: `0.1.15`
- `npm pack --dry-run`: package `@neurondev/ndx@0.1.15`, tarball
  `neurondev-ndx-0.1.15.tgz`, shasum
  `f71d8a2b0cfc9a6b1d68778b51bac245da000ec7`, total files `181`.
- `npm publish`: failed with `ECONNRESET` after three PUT attempts to
  `https://verdaccio.neurondev.net/@neurondev%2fndx`.
- After the failed publish, Verdaccio latest remained `0.1.14`.
- `npm ping`: failed with `ECONNRESET` before TLS connection establishment.
- `curl -I --max-time 20 https://verdaccio.neurondev.net/`: failed with
  `OpenSSL SSL_connect: SSL_ERROR_SYSCALL`.
- Retry `npm ping`: passed with `PONG 226ms`.
- Retry `npm publish`: completed with `+ @neurondev/ndx@0.1.15`.
- After retry publish, Verdaccio latest for `@neurondev/ndx` was `0.1.15` and
  the version list included `0.1.15`.
- Isolated install completed with `added 1 package`.
- Installed binary versions:
  - `ndx --version`: `0.1.15`
  - `ndxserver --version`: `0.1.15`

## Failures

- Initial Verdaccio publish failed because the registry endpoint stopped
  accepting TLS connections during publish verification. A later retry
  succeeded.

## Reproducibility

Run from `/mnt/f/dev/ndx`:

```bash
npm ping --registry https://verdaccio.neurondev.net/
npm publish --registry https://verdaccio.neurondev.net/
npm view @neurondev/ndx version versions --registry https://verdaccio.neurondev.net/ --json
npm install -g @neurondev/ndx@0.1.15 --registry https://verdaccio.neurondev.net/ --prefix /tmp/ndx-install-verify-015
/tmp/ndx-install-verify-015/bin/ndx --version
/tmp/ndx-install-verify-015/bin/ndxserver --version
```

## Locator Outcome

Not applicable; this release verification has no browser surface.
