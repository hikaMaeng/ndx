# Test Report: verdaccio-release
## Timestamp
2026-05-03 22:02:43 KST

## Environment
- Branch: `codex/docs-config-refactor`
- Commit: `e5c190f`
- Package: `@neurondev/ndx@0.1.13`
- Registry: `https://verdaccio.neurondev.net/`
- npm identity: `codexmon5kiq2`
- Docker: `Docker version 29.3.1, build c2be9cc`
- Docker Compose: `Docker Compose version v5.1.0`

## Executed Steps
1. Queried Verdaccio before publish:
   `npm view @neurondev/ndx version versions --registry https://verdaccio.neurondev.net/ --json`.
2. Ran `npm run deploy`.
3. Verified built CLI output:
   `node dist/src/cli/main.js --version` and
   `node dist/src/cli/main.js --mock --version`.
4. Ran `npm pack --dry-run --registry https://verdaccio.neurondev.net/`.
5. Published with `npm publish --registry https://verdaccio.neurondev.net/`.
6. Queried Verdaccio after publish.
7. Installed into `/tmp/ndx-install-verify-013`:
   `npm install -g @neurondev/ndx@0.1.13 --registry https://verdaccio.neurondev.net/ --prefix /tmp/ndx-install-verify-013`.
8. Verified installed binaries:
   `/tmp/ndx-install-verify-013/bin/ndx --version` and
   `/tmp/ndx-install-verify-013/bin/ndxserver --version`.

## Observed Logs
- Before publish, Verdaccio latest for `@neurondev/ndx` was `0.1.12`.
- Local `package.json` version was `0.1.13`.
- `npm run deploy` passed:
  - Node test summary: `64` tests, `64` pass, `0` fail.
  - Compose rebuilt `hika00/ndx-sandbox:0.1.0` with no cache.
  - Compose started `ndx-ndx-sandbox-1`, verified the sandbox write command,
    then removed the container and `ndx_default` network.
- Built binary versions:
  - `node dist/src/cli/main.js --version`: `0.1.13`
  - `node dist/src/cli/main.js --mock --version`: `0.1.13`
- `npm pack --dry-run` reported:
  - package: `@neurondev/ndx@0.1.13`
  - filename: `neurondev-ndx-0.1.13.tgz`
  - package size: `136.1 kB`
  - unpacked size: `660.1 kB`
  - shasum: `cc3706195be0c3b1e7d31ea9c5f226754620743a`
  - total files: `178`
- Publish completed with:
  `+ @neurondev/ndx@0.1.13`.
- After publish, Verdaccio latest for `@neurondev/ndx` was `0.1.13` and the
  version list included `0.1.13`.
- Isolated install added `1` package.
- Installed binary versions:
  - `ndx --version`: `0.1.13`
  - `ndxserver --version`: `0.1.13`

## Failures
None.

One test log line still printed `ndx-ts-session-server 0.1.12`, but follow-up
inspection showed that value came from existing test fixtures. The built and
installed binaries both reported `0.1.13`.

## Reproducibility
Run the plan from `/mnt/f/dev/ndx` with Verdaccio credentials available. Use an
isolated npm prefix for install verification so global user tools are not
modified.

## Locator Outcome
N/A. No dashboard browser surface changed or required verification for this
release task.
