# Test Report: settings-loader

## Timestamp

2026-04-26 20:30:00 KST

## Environment

- OS: Ubuntu 24.04 on WSL2
- Node: 22.22.2
- Docker: 29.3.1
- Compose: v5.1.0

## Executed Steps

1. Ran `npm test`.
2. Ran `npm run deploy`.
3. Ran `docker compose up -d --build ndx-agent`.
4. Ran `docker compose exec -T ndx-agent ndx --help`.
5. Ran `docker compose exec ndx-agent ndx` with a TTY and exited with `/exit`.

## Observed Logs

- Host `npm test`: TypeScript build succeeded; Node TAP reported 4 passing tests, 0 failures.
- Docker deploy: image `ndx-agent:local` built, in-container tests passed, and mock shell tool wrote `tmp/ndx-docker-verify.txt` with `verified`.
- Mock run printed `[config] /workspace/.ndx/settings.json`, proving Docker no longer depends on `.env` or `OPENAI_*` for model/provider config.
- `ndx --help` documented `/home/.ndx/settings.json`, nearest project `.ndx/settings.json`, and `/home/.ndx/search.json`.
- TTY startup printed `[config] /workspace/.ndx/settings.json`, `model: qwen3.6-35b-a3b:tr`, and `ndx>`.

## Failures

None.

## Reproducibility

Run `npm test`, `npm run deploy`, then `docker compose up -d --build ndx-agent` from the repository root.

## Locator Outcome

Not applicable. This package has no browser UI.

## Host Global Directory Note

- Attempted to create `/home/.ndx/search.json` on the host for local global search rules.
- The host denied creation with `Permission denied`; this is expected when `/home` is not writable by the current user.
- The loader still targets `/home/.ndx/search.json`; creating that host-global file requires appropriate local permissions.
