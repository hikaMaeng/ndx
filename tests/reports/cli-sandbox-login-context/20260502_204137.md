# Test Report: cli-sandbox-login-context

## Timestamp

2026-05-02 20:41:37 +0900

## Environment

- Workspace: `/mnt/f/dev/ndx`
- Shell: Bash
- Package: `@neurondev/ndx@0.1.7`
- Docker image built by deploy: `hika00/ndx-sandbox:0.1.0`

## Executed Steps

1. Ran `yarn build`.
2. Ran targeted tests:
   `node --test dist/tests/cli-session-client.test.js dist/tests/runtime.test.js dist/tests/tools.test.js dist/tests/tool-orchestration.test.js dist/tests/session-server.test.js`.
3. Ran `yarn test`.
4. Ran `npm run deploy`.
5. Ran a direct Docker tool smoke using `SessionServer` with
   `requireDockerSandbox: true` and `MockModelClient`, then verified a tool call
   wrote `tmp/docker-tool.txt` into the host workspace mount.

## Observed Logs

- Targeted tests: 29 passed, 0 failed.
- Model adapter focused rerun: 12 passed, 0 failed, including provider
  operational instruction coverage.
- Full suite during deploy: 62 passed, 0 failed.
- Deploy: build and full tests passed, compose removed previous containers,
  rebuilt `hika00/ndx-sandbox:0.1.0`, started `ndx-ndx-sandbox-1`, executed the
  `/workspace/tmp/ndx-docker-verify.txt` write, then removed the container and
  `ndx_default` network.
- Docker tool smoke printed:
  `docker tool smoke ok: .../workspace/tmp/docker-tool.txt`.

## Failures

None.

## Reproducibility

Run from `/mnt/f/dev/ndx`:

```bash
yarn build
node --test dist/tests/cli-session-client.test.js dist/tests/runtime.test.js dist/tests/tools.test.js dist/tests/tool-orchestration.test.js dist/tests/session-server.test.js
yarn test
npm run deploy
```

For the direct Docker tool smoke, instantiate `SessionServer` from `dist` with
`requireDockerSandbox: true`, `MockModelClient`, a temporary workspace, and ask
the mock prompt to create `tmp/docker-tool.txt`.

## Locator Outcome

Not a browser-facing change. No dashboard markup changed; existing dashboard
locator tests remain covered by `session-server.test.js`.
