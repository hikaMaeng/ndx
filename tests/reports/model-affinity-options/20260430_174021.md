# Test Report: model-affinity-options

## Timestamp

2026-04-30 17:40:21 +0900

## Environment

- Workspace: `/mnt/f/dev/ndx`
- Shell: bash
- Commands: `npm test`, `npm run deploy`

## Executed Steps

1. Built TypeScript with `tsc -p tsconfig.json` through `npm test`.
2. Ran the full Node test suite from `dist/tests/*.test.js`.
3. Exercised config loader coverage for object model catalogs with aliases,
   effort, thinking, and sampling options.
4. Exercised model adapter coverage for provider request option emission.
5. Exercised session-server `/model` and `/status` live state behavior.
6. Ran Docker deploy verification from pushed branch `codex/prefixperformance`.

## Observed Logs

- `npm run build`: passed.
- `node --test dist/tests/*.test.js`: 49 tests passed.
- `npm run deploy`: passed.
- Docker build cloned `https://github.com/hikaMaeng/ndx.git@codex/prefixperformance`.
- In-container `npm test`: 49 tests passed.
- In-container mock CLI created `tmp/ndx-docker-verify.txt` with `verified`.
- Compose removed the `ndx_default` network at the end of the deploy script.
- Router coverage now expects sticky model reuse for repeated selected-pool
  requests instead of per-request rotation.

## Failures

None in the final run.

## Reproducibility

Run `npm test` or `npm run deploy` from `/mnt/f/dev/ndx`. Deploy requires the
current branch to be pushed because the Dockerfile clones the branch from GitHub.

## Residual Risk

Browser verification is not applicable. The changed surface is config, routing,
model adapter payloads, and session slash commands; there is no browser-rendered
UI.
