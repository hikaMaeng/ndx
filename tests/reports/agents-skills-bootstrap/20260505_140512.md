# AGENTS.md And Skills Bootstrap Report

## Scope

- Implemented upstream-compatible AGENTS.md cascade behavior for global and
  project docs.
- Implemented skill discovery, model-visible skill summaries, explicit skill
  injection, duplicate suppression, and ambiguous-name protection.
- Verified both user skill roots and `$NDX_HOME/skills/.system` system roots.
- Verified session reload/source records include AGENTS.md and `SKILL.md`
  sources.

## Commands

- `yarn install --immutable`
- `yarn build`
- `node --test dist/tests/config.test.js dist/tests/agent.test.js dist/tests/session-server.test.js`
- `yarn test`
- `npm run deploy`

## Browser Verification

- Target: `http://127.0.0.1:45624/dashboard`
- Workspace fixture: `/tmp/ndx-browser-agents-skill`
- Browser: Chromium through Playwright, headless.
- Locator strategy:
  - `getByRole("main", { name: /server dashboard/i })`
  - `getByRole("navigation", { name: "Dashboard views" })`
  - role buttons `Overview`, `Session Logs`, `Users`, `Reload`, and `Exit`
  - `getByTestId("dashboard-bootstrap")`
  - `getByTestId("dashboard-sources")`
- Assertions:
  - bootstrap list contained `skills`
  - sources contained `AGENTS.md`
  - sources contained `SKILL.md`
  - browser-context `fetch("/api/reload", { method: "POST" })` returned
    `ok: true` and included the skill source
- Screenshot: `/tmp/ndx-dashboard-browser.png`

## Result

- Focused tests: 32 passed, 0 failed.
- Full tests inside deploy: 82 passed, 0 failed.
- Deploy: passed after the final system-skill-root change. The deploy path
  completed local build, full test suite,
  `docker compose down --remove-orphans`, no-cache `ndx-sandbox` image build,
  compose up, sandbox file-write verification, and final compose down.
- Browser verification: passed after the final deploy.
