# Test Report: codex-core-agent-loop-docs

## Timestamp

2026-04-29 02:25:29 KST

## Environment

- Workspace: `/mnt/f/dev/ndx`
- Shell: `bash`
- Command: `npm run deploy`
- Docker image target: `ndx-agent:local`

## Executed Steps

1. Listed `codex-rs/core/docs`.
2. Searched docs for `system-remainder`, `Agent loop internals`, `탈출 조건`, `hook 삽입점`.
3. Ran `npm run deploy`.

## Observed Logs

Documentation files found:

- `codex-rs/core/docs/agent-loop.md`
- `codex-rs/core/docs/api.md`
- `codex-rs/core/docs/architecture.md`
- `codex-rs/core/docs/constraints.md`
- `codex-rs/core/docs/internals.md`
- `codex-rs/core/docs/overview.md`
- `codex-rs/core/docs/testing.md`
- `codex-rs/core/docs/usage.md`

Marker search found:

- `docs/agent-loop.md`: `# Agent loop internals`
- `docs/agent-loop.md`: `## 탈출 조건`
- `docs/agent-loop.md`: `<system-remainder>` parser/injection path is not present
- `docs/agent-loop.md`: `## hook 삽입점과 영향`
- `docs/constraints.md`: no generic `<system-remainder>` parser exists

Deploy output:

- Root `npm run build` completed with `tsc -p tsconfig.json`.
- `docker compose down --remove-orphans` ran before build.
- Docker built `ndx-agent:local`.
- Containerized `npm test` ran 11 Node subtests.
- TAP summary: `# pass 11`, `# fail 0`.
- Mock verification ran `ndx --mock "create a file named tmp/ndx-docker-verify.txt with text verified"`.
- Shell tool output read `verified`.
- Final `docker compose down --remove-orphans` removed `ndx_default`.

## Failures

None observed.

## Reproducibility

Run from `/mnt/f/dev/ndx`:

```bash
npm run deploy
```

The command rebuilds the Docker image without cache and depends on network access to clone `https://github.com/hikaMaeng/ndx.git` at the current branch name.

## Locator Outcome

Not applicable. No browser verification was performed because this task only added documentation and runtime-log verification.

