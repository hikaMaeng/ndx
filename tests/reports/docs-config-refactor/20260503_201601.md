# Test Report: docs-config-refactor

## Timestamp

2026-05-03 20:16:01 +0900

## Environment

- Workspace: `/mnt/f/dev/ndx`
- Branch: `codex/docs-config-refactor`
- Package: `@neurondev/ndx@0.1.13`
- Sandbox image built by deploy: `hika00/ndx-sandbox:0.1.0`
- Dashboard target: `http://127.0.0.1:45242/dashboard`

## Executed Steps

1. Ran `yarn build && yarn test`.
2. Ran `npm run deploy`.
3. After final default-string cleanup, ran `npm run deploy` again.
4. Started `node dist/src/cli/main.js serve --mock --cwd /tmp/ndx-docs-config-refactor-browser --listen 127.0.0.1:45241 --dashboard-listen 127.0.0.1:45242`.
5. Fetched dashboard HTML and asserted the documented DOM contract.
6. Ran Playwright screenshot verification with selector `main[aria-labelledby="dashboard-title"][data-testid="ndx-dashboard"]`.
7. Confirmed `docs/` contains only the required seven package docs.

## Observed Logs

- `yarn build && yarn test`: passed, 64 tests passed.
- Final `npm run deploy`: passed; it ran build, 64 tests, compose down,
  no-cache `ndx-sandbox` build, compose up, sandbox write to
  `/workspace/tmp/ndx-docker-verify.txt`, and compose teardown.
- Mock server printed:
  - `[session-server] ws://127.0.0.1:45241`
  - `[dashboard] http://127.0.0.1:45242`
- Dashboard fetch returned HTTP 200.
- Playwright wrote `/tmp/ndx-docs-config-refactor-dashboard.png`.

## Failures

No command failed. Node emitted the expected experimental `node:sqlite` warning
during tests and server startup.

## Reproducibility

Run from repository root:

```bash
yarn build && yarn test
npm run deploy
node dist/src/cli/main.js serve --mock --cwd /tmp/ndx-docs-config-refactor-browser --listen 127.0.0.1:45241 --dashboard-listen 127.0.0.1:45242
playwright screenshot --browser chromium --viewport-size 1280,720 --wait-for-selector 'main[aria-labelledby="dashboard-title"][data-testid="ndx-dashboard"]' http://127.0.0.1:45242/dashboard /tmp/ndx-docs-config-refactor-dashboard.png
```

## Locator Outcome

- `main[aria-labelledby="dashboard-title"][data-testid="ndx-dashboard"]`: passed in Playwright.
- `aside aria-label="Dashboard menu"`: present in DOM fetch.
- `nav aria-label="Server actions"`: present in DOM fetch.
- `Reload` and `Exit` buttons: present in DOM fetch.
- `role="status" data-testid="dashboard-action-status"`: present in DOM fetch.
- `data-testid="dashboard-sources"`: present in DOM fetch.
- `data-testid="dashboard-bootstrap"`: present in DOM fetch.
