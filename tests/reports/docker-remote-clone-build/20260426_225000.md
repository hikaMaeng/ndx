# Test Report: docker-remote-clone-build

## Timestamp

2026-04-26 22:50:00 KST

## Environment

- OS: Ubuntu 24.04 on WSL2
- Node: 22.22.2
- Docker: 29.3.1
- Compose: v5.1.0
- Git branch: `codex/docker-remote-clone-build`
- Verified commit: `1205f84`

## Executed Steps

1. Ran `npm test` locally before committing.
2. Committed `1205f84 chore: build docker image from remote branch`.
3. Pushed `codex/docker-remote-clone-build` to `origin`.
4. Ran `npm run deploy`.
5. Ran `NDX_GIT_REF=codex/docker-remote-clone-build NDX_GIT_CACHE_BUST=$(git rev-parse HEAD) docker compose up -d ndx-agent`.
6. Ran `docker compose exec -T ndx-agent ndx --help`.

## Observed Logs

- Local `npm test`: TypeScript build succeeded; Node TAP reported 4 passing tests, 0 failures.
- Git push: `codex/docker-remote-clone-build` created on `origin` and configured as upstream.
- Docker build printed `Building ndx from https://github.com/hikaMaeng/ndx.git@codex/docker-remote-clone-build (1205f8475919d3f28dc9b9b3c95ba09d2e761a78)`.
- Dockerfile clone step succeeded with `git clone --depth 1 --branch "codex/docker-remote-clone-build"`.
- In-container `npm test`: TypeScript build succeeded; Node TAP reported 4 passing tests, 0 failures.
- In-container mock agent printed `[config] /workspace/.ndx/settings.json`, executed the shell tool, and wrote `verified`.
- `ndx --help` printed the settings contract and interactive mode help from `/usr/local/bin/ndx`.

## Failures

None.

## Reproducibility

Run from the repository root after pushing the feature branch:

```bash
npm run deploy
NDX_GIT_REF=codex/docker-remote-clone-build NDX_GIT_CACHE_BUST=$(git rev-parse HEAD) docker compose up -d ndx-agent
docker compose exec -T ndx-agent ndx --help
```

## Locator Outcome

Not applicable. This package has no browser UI.
