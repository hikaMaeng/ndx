# Test Report: source-layout

## Timestamp

2026-04-29 11:05:48 KST

## Environment

- Workspace: `/mnt/f/dev/ndx`
- Shell: bash

## Executed Steps

1. Moved root `src/*.ts` files into role folders.
2. Updated imports, package bin path, Docker symlink path, tests, and docs.
3. Ran `npm run build`.
4. Ran `npm test`.
5. Ran CLI smoke with `node dist/src/cli/main.js --mock "list files"`.
6. Pushed `codex/runtime-event-protocol` so Docker deploy could clone the moved source layout.
7. Ran `npm run deploy`.

## Observed Logs

- `npm run build`: passed.
- `npm test`: 15 tests passed.
- CLI smoke completed and printed `mock agent completed`.
- `npm run deploy`: passed local build, compose refresh, Docker image build from the pushed branch, in-container 15-test run, in-container mock agent execution, and compose teardown.
- `src` top level now contains role folders: `agent`, `cli`, `config`, `model`, `runtime`, `session`, `shared`, `tools`.

## Failures

No command failed. Host CLI smoke reported expected persistence drop logs because `/home/.ndx` was not writable in the host environment.

## Reproducibility

Run `npm run build`, `npm test`, CLI smoke, and `npm run deploy` from `/mnt/f/dev/ndx`.

## Locator Outcome

No browser UI exists for this package.
