# Test Report: dashboard-webserver

## Timestamp

2026-05-02 18:18:12 KST

## Environment

- Workspace: `/mnt/f/dev/ndx`
- Branch: `codex/dashboard-webserver`
- Node: `v22.22.2`
- Playwright CLI: `1.59.1`
- Browser target: `http://127.0.0.1:45233/dashboard`

## Executed Steps

1. Ran `yarn build`.
2. Ran `node --test dist/tests/session-server.test.js dist/tests/cli-session-client.test.js`.
3. Ran `yarn test`.
4. Ran `npm run deploy`.
5. Created `/tmp/ndx-dashboard-browser` with `.ndx/settings.json` and `AGENTS.md`.
6. Started `node dist/src/cli/main.js serve --mock --cwd /tmp/ndx-dashboard-browser --listen 127.0.0.1:45232 --dashboard-listen 127.0.0.1:45233`.
7. Ran Playwright screenshot verification with `--wait-for-selector 'main[aria-labelledby="dashboard-title"][data-testid="ndx-dashboard"]'`.
8. Ran DOM/API checks for the dashboard menu, Reload/Exit buttons, status role, AGENTS source, and `POST /api/reload`.
9. Ran `POST /api/exit` and confirmed the server process exited.

## Observed Logs

- `yarn build`: passed.
- Focused Node tests: 18 passed.
- First `yarn test`: one timing-sensitive `tool-orchestration` parallel scheduling assertion failed; the same test passed when rerun by name.
- Second `yarn test`: 57 passed, 0 failed.
- `npm run deploy`: passed; local build and tests passed, Compose removed prior containers, rebuilt `hika00/ndx-sandbox:0.1.0`, started `ndx-sandbox`, wrote `/workspace/tmp/ndx-docker-verify.txt`, and tore Compose down.
- Mock server printed `[session-server] ws://127.0.0.1:45232` and `[dashboard] http://127.0.0.1:45233`.
- Playwright wrote `/tmp/ndx-dashboard-verification.png`.

## Failures

- Initial full-test run had one transient scheduling failure in `tool-orchestration`; focused rerun and full rerun both passed.
- A temporary Playwright Test import attempt failed because `@playwright/test` was not available as a repository dependency. Verification used the Playwright CLI screenshot command plus a Node DOM/API check instead.

## Reproducibility

The verified path is reproducible with committed source plus:

```bash
yarn build
node --test dist/tests/session-server.test.js dist/tests/cli-session-client.test.js
yarn test
npm run deploy
npx playwright screenshot --browser chromium --viewport-size 1280,720 --wait-for-selector 'main[aria-labelledby="dashboard-title"][data-testid="ndx-dashboard"]' http://127.0.0.1:45233/dashboard /tmp/ndx-dashboard-verification.png
```

## Locator Outcome

- `main[aria-labelledby="dashboard-title"][data-testid="ndx-dashboard"]`: passed in Playwright.
- `aside aria-label="Dashboard menu"`: present in DOM check.
- `nav aria-label="Server actions"`: present in DOM check.
- Buttons `Reload` and `Exit`: present in DOM check.
- `role="status" data-testid="dashboard-action-status"`: present in DOM check.
- `data-testid="dashboard-sources"` contained `AGENTS.md` before and after Reload.
