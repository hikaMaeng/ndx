# Test Report: docker-tool-root-audit

## Timestamp

2026-05-03 13:45:30 +09:00

## Environment

- OS shell: bash
- Workspace: `/mnt/f/dev/ndx`
- Package: `@neurondev/ndx@0.1.12`
- Registry: `https://verdaccio.neurondev.net/`
- Sandbox image: `hika00/ndx-sandbox:0.1.0`

## Executed Steps

1. Updated core tool path handling so `/root` maps to the active tool cwd.
2. Added sandbox external-tool audit records under
   `/home/.ndx/system/logs/tool-executions.jsonl` and mirrored them to Docker
   container stdout.
3. Ran `yarn build`.
4. Ran `node --test dist/tests/tools.test.js dist/tests/cli-session-client.test.js`.
5. Ran a direct Docker sandbox smoke test using `shell` with `cwd: "/root"`.
6. Ran `yarn test`.
7. Ran `npm run deploy`.
8. Ran `npm publish --registry https://verdaccio.neurondev.net/`.
9. Ran isolated install verification for `@neurondev/ndx@0.1.12`.

## Observed Logs

- Focused tests: 14 tests passed, 0 failed.
- Direct Docker smoke summary:
  - inner cwd: `/workspace`
  - host file existed: `true`
  - host file text: `ok`
  - audit JSONL lines inspected: `2`
  - Docker log lines inspected: `2`
  - redacted env marker present: `true`
  - raw `NDX_TOOL_ARGS={...}` env payload present in audit args: `false`
- Full `yarn test`: 64 tests passed, 0 failed.
- `npm run deploy`: passed; build and tests passed, Compose rebuilt
  `hika00/ndx-sandbox:0.1.0`, started `ndx-sandbox`, wrote
  `/workspace/tmp/ndx-docker-verify.txt`, and removed the container/network.
- Verdaccio publish completed with `+ @neurondev/ndx@0.1.12`; tarball shasum
  `0768921522984ab9360c446c0a1355a3c3cb99ae`.
- Install verification:
  - `ndx --version`: `0.1.12`
  - `ndxserver --version`: `0.1.12`

## Failures

- The first ad hoc Docker smoke attempt failed because the inline config omitted
  `projectMcp` and `globalMcp`. The smoke script was corrected and rerun
  successfully.

## Reproducibility

```bash
yarn build
node --test dist/tests/tools.test.js dist/tests/cli-session-client.test.js
yarn test
npm run deploy
npm publish --registry https://verdaccio.neurondev.net/
npm install -g @neurondev/ndx@0.1.12 --registry https://verdaccio.neurondev.net/ --prefix /tmp/ndx-install-verify-012
/tmp/ndx-install-verify-012/bin/ndx --version
/tmp/ndx-install-verify-012/bin/ndxserver --version
```

## Locator Outcome

Not applicable. No browser UI was changed.
