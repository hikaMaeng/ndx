# Test Report: typescript-agent

## Timestamp

2026-04-26 19:51:42 KST

## Environment

- OS: Ubuntu 24.04 on WSL2
- Node: 22.22.2
- pnpm: 10.33.0
- Docker: 29.3.1
- Compose: v5.1.0

## Executed Steps

1. Ran `pnpm install --registry=https://registry.npmjs.org` after the default Verdaccio registry failed with `ECONNRESET`.
2. Ran `npm test` on the host.
3. Ran `npm run deploy`.

## Observed Logs

- Host `npm test`: TypeScript build succeeded; Node TAP reported 4 passing tests, 0 failures.
- Docker build: `ndx-agent:local` built from `node:22-bookworm-slim`.
- Docker `npm test`: TypeScript build succeeded; Node TAP reported 4 passing tests, 0 failures.
- Docker mock agent: emitted `[tool:shell]` for creating `tmp/ndx-docker-verify.txt` and `[tool:result]` with `exitCode: 0`, `stdout: "verified"`, `timedOut: false`.

## Failures

- Initial `pnpm install` failed because `https://verdaccio.neurondev.net` reset TLS connections.
- No build, unit, Docker, or mock-agent failures after using the public npm registry.

## Reproducibility

Run `npm test` and `npm run deploy` from the repository root. If Verdaccio is unavailable, first run `pnpm install --registry=https://registry.npmjs.org`.

## Locator Outcome

Not applicable. This package has no browser UI.

## Local Model Compose Run

- Updated `.env` for `OPENAI_BASE_URL=http://192.168.0.6:12345/v1` and `NDX_MODEL=qwen3.6-35b-a3b:mm`.
- Ran `docker compose build ndx-agent && docker compose up --abort-on-container-exit ndx-agent`.
- Container returned `네, 준비 완료했습니다!` and exited with code 0.
