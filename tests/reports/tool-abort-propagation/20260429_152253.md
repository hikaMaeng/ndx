# Test Report: tool-abort-propagation
## Timestamp
2026-04-29 15:22:53 +0900

## Environment
- OS shell: bash
- Package: root `ndx`
- Command: `npm test`

## Executed Steps
1. Built TypeScript with `tsc -p tsconfig.json` through `npm test`.
2. Ran Node test suite with `node --test dist/tests/*.test.js`.
3. Started `npm run deploy` after pushing the branch for the GitHub clone build.
4. Observed that the first deploy attempt built the image and passed in-container tests, but `docker compose run` did not return after `npm test` because the service is configured with `tty: true`.
5. Stopped the hung run and removed the compose container/network with `docker compose down --remove-orphans`.
6. Updated deploy verification containers to use `docker compose run -T`.
7. Re-ran `npm test`.
8. Pushed the deploy fix and re-ran `npm run deploy` against the GitHub branch.

## Observed Logs
- TAP summary: `tests 28`, `pass 28`, `fail 0`.
- New coverage: `agent abort signal propagates to external tool processes`.
- Earlier draft attempts exposed a timing gap in the test fixture, then passed after waiting for the external tool ready marker before aborting.
- Docker build cloned `https://github.com/hikaMaeng/ndx.git@codex/process-manager-library`.
- In-container TAP summary before the deploy hang: `tests 28`, `pass 28`, `fail 0`.
- Deploy hang root cause: verification `docker compose run` inherited interactive service TTY settings.
- Final local TAP summary after the deploy script fix: `tests 28`, `pass 28`, `fail 0`.
- Final deploy TAP summary inside Docker: `tests 28`, `pass 28`, `fail 0`.
- Final mock-agent verification wrote `verified` to `/workspace/tmp/ndx-docker-verify.txt`.
- Final compose cleanup removed `ndx_default`.

## Failures
None in the final `npm test` or final `npm run deploy` run. First deploy attempt hung after successful in-container tests; the deploy command now uses `docker compose run -T` for verification containers.

## Reproducibility
Run `npm test` or `npm run deploy` from `/mnt/f/dev/ndx`. The abort propagation test uses a temporary global `.ndx` tool directory and does not require external model credentials. Deploy requires Docker and network access to clone the pushed GitHub branch.

## Locator Outcome
No browser UI exists for this package. Browser locator verification was not run.
