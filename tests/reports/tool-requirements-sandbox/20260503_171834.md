# Test Report: tool-requirements-sandbox

## Timestamp

2026-05-03 17:18:34 +0900

## Environment

- Workspace: `/mnt/c/Users/hika0/.codex/worktrees/2b5c/ndx`
- Branch: `codex/toolanddocker`
- Package version: `0.1.14`
- Docker image built locally: `hika00/ndx-sandbox:0.1.1`
- Docker Hub digest:
  `sha256:e44a16254deef48c42fc946ea7a36be352b0e9dc6c9a74ea36a1faddc5dc20a5`

## Executed Steps

1. Ran `yarn install --immutable`.
2. Ran `yarn build`.
3. Ran targeted tests:
   `node --test dist/tests/config.test.js dist/tests/tools.test.js dist/tests/cli-workspace.test.js`.
4. Ran full suite with `yarn test`.
5. Ran final deploy verification with `npm run deploy` after the package
   version update.
6. Ran direct image smoke for Node, Python, Git, Patch, ripgrep, jq,
   Playwright, and Chromium launch.
7. Pushed `hika00/ndx-sandbox:0.1.1` to Docker Hub.
8. Pulled `hika00/ndx-sandbox:0.1.1` from Docker Hub and reran the same direct
   image smoke.
9. Ran `yarn build && node --test dist/tests/config.test.js
dist/tests/tools.test.js dist/tests/cli-workspace.test.js` after formatting.

## Observed Logs

- `yarn install --immutable`: completed with Yarn PnP ESM loader warning only.
- `yarn build`: passed.
- Targeted `node --test`: 21 tests passed.
- `yarn test`: 66 tests passed.
- Final `npm run deploy`: 66 tests passed, Compose removed prior resources,
  rebuilt `hika00/ndx-sandbox:0.1.1` with Playwright Chromium runtime,
  started `ndx-sandbox`, wrote `/workspace/tmp/ndx-docker-verify.txt`, and
  tore Compose down.
- Direct image smoke output included:
  `v22.22.2`, `Python 3.11.2`, `git version 2.39.5`,
  `GNU patch 2.7.6`, `ripgrep 13.0.0`, `jq-1.6`, `Version 1.59.1`,
  and `chromium-ok`.
- `docker push hika00/ndx-sandbox:0.1.1` completed with digest
  `sha256:e44a16254deef48c42fc946ea7a36be352b0e9dc6c9a74ea36a1faddc5dc20a5`.
- `docker pull hika00/ndx-sandbox:0.1.1` reported the image up to date at the
  same digest, and the post-pull smoke again ended with `chromium-ok`.
- Post-format build plus targeted tests passed with 21 tests.

## Failures

- First direct Playwright module smoke failed because `npm install -g
playwright` made the CLI available but not `require("playwright")` from tool
  code. `NODE_PATH=/usr/local/lib/node_modules` was added to the sandbox image,
  then deploy and direct smoke were rerun successfully.

## Reproducibility

Run from `/mnt/c/Users/hika0/.codex/worktrees/2b5c/ndx`:

```bash
yarn install --immutable
yarn build
node --test dist/tests/config.test.js dist/tests/tools.test.js dist/tests/cli-workspace.test.js
yarn test
npm run deploy
docker run --rm hika00/ndx-sandbox:0.1.1 bash -lc 'playwright --version'
docker push hika00/ndx-sandbox:0.1.1
docker pull hika00/ndx-sandbox:0.1.1
```

## Locator Outcome

Not applicable. This package has no browser UI for this change.
