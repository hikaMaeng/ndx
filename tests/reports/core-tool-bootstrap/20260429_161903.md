# Test Report: core-tool-bootstrap

## Timestamp

2026-04-29 16:19:03 +09:00

## Environment

- Host workspace: `/mnt/f/dev/ndx`
- Branch: `codex/externaltool`
- Docker status: feature-branch deploy passed; PR merge and main deploy pending

## Executed Steps

1. Added built-in core tool package definitions for `shell`, `apply_patch`,
   `list_dir`, `view_image`, `web_search`, `image_generation`, `tool_suggest`,
   `tool_search`, and `request_permissions`.
2. Updated `ensureGlobalNdxHome()` to install every built-in package under
   `/home/.ndx/core/tools`.
3. Updated the external tool runner to pass core/global/project tool directory
   paths and websearch settings through environment variables.
4. Updated docs for bootstrap and tool ownership contracts.
5. Ran `npm test`.
6. Pushed `codex/externaltool` to GitHub.
7. Ran `npm run deploy` on `codex/externaltool`.

## Observed Logs

- `npm test`: 30 tests passed.
- `npm run deploy` on `codex/externaltool`: passed local build, compose
  teardown, no-cache image build from
  `https://github.com/hikaMaeng/ndx.git@codex/externaltool`, in-container
  30-test run, mock agent verification, and compose teardown.
- Startup/session bootstrap log reported 32 existing elements under
  `/home/.ndx`, including `core shell`, `core apply_patch`, `core list_dir`,
  `core view_image`, `core web_search`, `core image_generation`,
  `core tool_suggest`, `core tool_search`, and `core request_permissions`
  tool, manifest, and runtime entries.
- New coverage:
  - `ensureGlobalNdxHome installs missing settings and core tool packages`
  - `registry exposes bootstrapped core capability tools as external tools`
  - direct external execution of `list_dir`
  - direct external execution of `tool_search`

## Failures

None so far.

## Reproducibility

Run from `/mnt/f/dev/ndx`:

```bash
npm test
```

Docker deploy was executed on the pushed feature branch:

```bash
npm run deploy
```

## Locator Outcome

No browser UI is rendered by this package.
