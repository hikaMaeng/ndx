# Test Report: managed-cli-detached-server

## Timestamp

2026-05-04 13:02:24 Asia/Seoul

## Environment

- Repository: `/mnt/f/dev/ndx`
- Shell: bash
- Node test runner with built `dist/`
- Docker compose deploy path
- Package version: `0.1.21`

## Executed Steps

1. Reviewed Windows logs showing detached PowerShell spawn succeeded but socket
   readiness stayed at `connect` until timeout.
2. Added launcher PID liveness to readiness wait logs.
3. Added timeout-time diagnostic log tailing for all managed diagnostic paths.
4. Changed the Windows wrapper to select a writable diagnostic log path before
   optionally redirecting server stdout/stderr there.
5. Expanded connection error descriptions with code, cause, and short stack
   detail when available.
6. Ran `yarn build`.
7. Ran `node --test dist/tests/cli-workspace.test.js`.
8. Ran `yarn test`.
9. Ran `npm run deploy`.

## Observed Logs

- `yarn build`: passed.
- Targeted test: 9 tests passed, including Windows launcher payload coverage for
  writable log selection and safe `*>> $logPath` server body capture.
- `yarn test`: 75 tests passed.
- `npm run deploy`: completed build, full tests, `docker compose down`,
  no-cache `hika00/ndx-sandbox:0.1.1` build, compose startup,
  `/workspace/tmp/ndx-docker-verify.txt` write, and compose teardown.

## Failures

None in local build, targeted test, full test, or deploy verification.

## Reproducibility

Run from `/mnt/f/dev/ndx`:

```bash
yarn build
node --test dist/tests/cli-workspace.test.js
yarn test
npm run deploy
```

On Windows timeout, collect the CLI stderr. It should now include
`[server-log]` tail lines when either managed diagnostic file exists.

## Locator Outcome

Not applicable; this behavior has no browser surface.
