# Test Report: managed-cli-detached-server

## Timestamp

2026-05-04 13:33:31 Asia/Seoul

## Environment

- Repository: `/mnt/f/dev/ndx`
- Shell: bash
- Node test runner with built `dist/`
- Docker compose deploy path
- Package version: `0.1.22`

## Executed Steps

1. Reviewed Windows logs showing the hidden PowerShell launcher process exits
   within about one second and neither managed diagnostic log is created.
2. Added a parent-side host log path for Windows detached launches:
   `%TEMP%\ndx-managed-server-host.log`.
3. Changed the CLI parent process to open the host log before spawning the
   hidden PowerShell launcher and redirect launcher stdout/stderr to that file.
4. Added the host log to timeout diagnostic tailing before the managed server
   logs.
5. Expanded Windows launcher metadata test coverage for the host log path.
6. Ran `yarn exec prettier --write`.
7. Ran `yarn build`.
8. Ran `node --test dist/tests/cli-workspace.test.js`.
9. Ran `yarn test`.
10. Ran `npm run deploy`.

## Observed Logs

- Formatting completed for the touched source, docs, plan, and test files.
- `yarn build`: passed.
- Targeted test: 9 tests passed, including Windows launcher payload coverage for
  the managed log paths and host log path.
- `yarn test`: 75 tests passed.
- `npm run deploy`: completed build, full tests, `docker compose down`,
  no-cache `hika00/ndx-sandbox:0.1.1` build, compose startup,
  `/workspace/tmp/ndx-docker-verify.txt` write, and compose teardown.

## Failures

None in local build, targeted test, full test, or deploy verification.

## Reproducibility

Run from `/mnt/f/dev/ndx`:

```bash
yarn exec prettier --write src/cli/main.ts src/cli/workspace.ts tests/cli-workspace.test.ts docs/architecture.md docs/internals.md docs/usage.md tests/plans/managed-cli-detached-server-20260504.md tests/plans/verdaccio-managed-server-release-20260504-022.md
yarn build
node --test dist/tests/cli-workspace.test.js
yarn test
npm run deploy
```

On Windows timeout, collect the CLI stderr. It should print the host log path and
tail it when the hidden launcher writes anything before exiting.

## Locator Outcome

Not applicable; this behavior has no browser surface.
