# Test Report: managed-cli-detached-server

## Timestamp

2026-05-04 05:05:06 Asia/Seoul

## Environment

- Repository: `/mnt/f/dev/ndx`
- Shell: bash
- Node test runner with built `dist/`
- Docker compose deploy path
- Package version: `0.1.20`

## Executed Steps

1. Added staged managed-server probes that report `connect`, `login`,
   `initialize`, or `server-name` failure instead of a bare boolean.
2. Added CLI stderr logging for detached launcher type, cwd, command, exec path,
   server args, diagnostic log paths, spawned pid, readiness poll attempts,
   failing stage, and last error.
3. Expanded Windows PowerShell host diagnostics to log cwd, executable, args,
   set-location success, body invocation, exit code, and failure detail.
4. Added Windows diagnostic fallback log path `%TEMP%\ndx-managed-server.log`
   when the primary managed log path is not writable.
5. Ran `yarn build`.
6. Ran `node --test dist/tests/cli-workspace.test.js`.
7. Ran `yarn test`.
8. Ran `npm run deploy`.

## Observed Logs

- `yarn build`: passed.
- Targeted test: 9 tests passed, including connection failure stage coverage and
  Windows launcher payload checks for detailed lifecycle logs plus temp fallback.
- `yarn test`: 75 tests passed.
- `npm run deploy`: completed build, full tests, `docker compose down`,
  no-cache `hika00/ndx-sandbox:0.1.1` build, compose startup,
  `/workspace/tmp/ndx-docker-verify.txt` write, and compose teardown.

## Failures

None in local build, targeted test, full test, or deploy verification.

## Reproducibility

Run from `/mnt/f/dev/ndx`:

```bash
yarn build
node --test dist/tests/cli-workspace.test.js
yarn test
npm run deploy
```

Actual Windows detached process survival still needs confirmation from a real
Windows PowerShell install. If startup times out, capture the CLI stderr and
then inspect `%USERPROFILE%\.ndx\system\logs\managed-server.log` and
`%TEMP%\ndx-managed-server.log`.

## Locator Outcome

Not applicable; this behavior has no browser surface.
