# Test Report: managed-cli-detached-server

## Timestamp

2026-05-04 03:20:59 Asia/Seoul

## Environment

- Repository: `/mnt/f/dev/ndx`
- Shell: bash
- Node test runner with built `dist/`
- Docker compose deploy path
- Package version: `0.1.17`

## Executed Steps

1. Reviewed the Windows timeout after installing `0.1.16`:
   `timed out waiting for detached ndx server: ws://127.0.0.1:45123`.
2. Replaced the Windows `Start-Process -ArgumentList` wrapper with an encoded
   hidden PowerShell host.
3. Added diagnostics to append server startup, exit, and exception details to
   `~/.ndx/system/logs/managed-server.log`.
4. Updated launcher unit coverage to decode and inspect the PowerShell payload.
5. Ran `yarn build`.
6. Ran `node --test dist/tests/cli-workspace.test.js`.
7. Ran `yarn test`.
8. Ran `npm run deploy`.

## Observed Logs

- `yarn build`: passed.
- Targeted test: 8 tests passed, including encoded Windows launcher payload,
  macOS `nohup`, Linux `setsid` with `nohup` fallback, and existing server
  attach before fallback.
- `yarn test`: 74 tests passed.
- `npm run deploy`: completed build, tests, `docker compose down`, no-cache
  sandbox build, compose startup, `/workspace/tmp/ndx-docker-verify.txt` write,
  and compose teardown.

## Failures

None in local build, unit, full test, or deploy verification.

## Reproducibility

Run from `/mnt/f/dev/ndx`:

```bash
yarn build
node --test dist/tests/cli-workspace.test.js
yarn test
npm run deploy
```

Actual Windows process survival still needs confirmation on a Windows host. If
startup times out again, inspect
`%USERPROFILE%\.ndx\system\logs\managed-server.log`.

## Locator Outcome

Not applicable; this behavior has no browser surface.
