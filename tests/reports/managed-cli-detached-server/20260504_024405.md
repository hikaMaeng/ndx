# Test Report: managed-cli-detached-server

## Timestamp

2026-05-04 02:44:05 Asia/Seoul

## Environment

- Repository: `/mnt/f/dev/ndx`
- Shell: bash
- Node test runner with built `dist/`
- Docker compose deploy path
- Package version: `0.1.16`

## Executed Steps

1. Reviewed the repeated Windows log where every `ndx` invocation reported
   `ws://127.0.0.1:45123 is not reachable`, meaning the prior managed server
   did not survive CLI exit.
2. Changed managed startup to build an OS-specific background launcher.
3. Added unit coverage for Windows, macOS, and Linux launcher command shapes.
4. Ran `yarn build`.
5. Ran `node --test dist/tests/cli-workspace.test.js`.
6. Ran `yarn test`.
7. Ran `npm run deploy`.

## Observed Logs

- `yarn build`: passed.
- Targeted test: 8 tests passed, including:
  - Windows launcher uses PowerShell `Start-Process`.
  - macOS launcher uses `nohup`.
  - Linux launcher uses `setsid` with `nohup` fallback.
  - Existing server attach still succeeds before fallback.
- `yarn test`: 74 tests passed.
- `npm run deploy`: completed build, tests, `docker compose down`, no-cache
  sandbox build, compose startup, `/workspace/tmp/ndx-docker-verify.txt` write,
  and compose teardown.

## Failures

None in local build, unit, full test, or deploy verification.

## Reproducibility

Run from `/mnt/f/dev/ndx`:

```bash
yarn build
node --test dist/tests/cli-workspace.test.js
yarn test
npm run deploy
```

Actual Windows process survival still needs confirmation on a Windows host
because this environment can only verify the generated Windows launcher command.

## Locator Outcome

Not applicable; this behavior has no browser surface.
