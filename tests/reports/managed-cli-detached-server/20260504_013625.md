# Test Report: managed-cli-detached-server

## Timestamp

2026-05-04 01:36:25 KST

## Environment

- Repository: `/mnt/f/dev/ndx`
- Shell: bash
- Node tests: `node --test`
- Docker image: `hika00/ndx-sandbox:0.1.1`
- Browser verification: Chromium through Playwright inside the sandbox image

## Executed Steps

1. Ran `yarn build`.
2. Ran `node --test dist/tests/cli-workspace.test.js`.
3. Ran `yarn test`.
4. Ran `npm run deploy`.
5. Re-ran `npm run deploy` after a transient test failure.
6. Started a mock `ndxserver` dashboard and verified dashboard locators with
   headless Chromium inside `hika00/ndx-sandbox:0.1.1`.

## Observed Logs

- `yarn build`: passed.
- `node --test dist/tests/cli-workspace.test.js`: 5 tests passed, 0 failed.
- `yarn test`: 70 tests passed, 0 failed.
- First `npm run deploy`: failed in `agent abort signal propagates to external
  tool processes`; the same test passed in the preceding `yarn test` and in the
  deploy rerun.
- Second `npm run deploy`: passed; build and tests passed, compose resources
  were removed, `ndx-sandbox` rebuilt no-cache, container started, sandbox write
  verification ran, and compose resources were removed.
- Browser check printed `NDX Dashboard` after locator verification.

## Failures

- One transient first-attempt deploy failure in the abort propagation test.
  Re-running the full deploy path passed without code changes.

## Reproducibility

Run from `/mnt/f/dev/ndx`:

```bash
yarn build
node --test dist/tests/cli-workspace.test.js
yarn test
npm run deploy
```

Browser locator check:

```bash
docker run --rm -v /mnt/f/dev/ndx:/workspace -w /workspace hika00/ndx-sandbox:0.1.1 bash -lc '<start mock ndxserver, curl dashboard, run Playwright locator checks>'
```

## Locator Outcome

Passed with:

- `main[aria-labelledby="dashboard-title"][data-testid="ndx-dashboard"]`
- complementary landmark named `Dashboard menu`
- navigation landmark named `Server actions`
- buttons named `Reload` and `Exit`
- `data-testid="dashboard-sources"`
- `data-testid="dashboard-bootstrap"`
