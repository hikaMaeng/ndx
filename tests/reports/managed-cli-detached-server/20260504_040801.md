# Test Report: managed-cli-detached-server

## Timestamp

2026-05-04 04:08:01 Asia/Seoul

## Environment

- Repository: `/mnt/f/dev/ndx`
- Shell: bash
- Node test runner with built `dist/`
- Docker compose deploy path
- Package version: `0.1.18`

## Executed Steps

1. Checked the Windows diagnostic path after another startup timeout; no
   `managed-server.log` existed under `/mnt/c/Users/hika0/.ndx/system/logs`.
2. Observed the existing logs directory was WSL-created and not writable by the
   normal mapped owner, making blocking diagnostic writes a plausible startup
   blocker.
3. Changed the Windows PowerShell launcher so diagnostic writes are best-effort.
4. Kept server execution independent from whether the diagnostic directory or
   file is writable.
5. Ran `yarn build`.
6. Ran `node --test dist/tests/cli-workspace.test.js`.
7. Ran `yarn test`.
8. Ran `npm run deploy`.

## Observed Logs

- `yarn build`: passed.
- Targeted test: 8 tests passed, including encoded Windows launcher payload and
  best-effort diagnostic helper.
- `yarn test`: 74 tests passed.
- `npm run deploy`: completed build, tests, `docker compose down`, no-cache
  sandbox build, compose startup, `/workspace/tmp/ndx-docker-verify.txt` write,
  and compose teardown.

## Failures

None in local build, unit, full test, or deploy verification.

## Reproducibility

Run from `/mnt/f/dev/ndx`:

```bash
yarn build
node --test dist/tests/cli-workspace.test.js
yarn test
npm run deploy
```

Actual Windows process survival still needs confirmation on a Windows host. If
startup times out again, inspect whether
`%USERPROFILE%\.ndx\system\logs\managed-server.log` exists; absence no longer
means the launcher stopped before server startup.

## Locator Outcome

Not applicable; this behavior has no browser surface.
