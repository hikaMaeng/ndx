# Test Report: managed-cli-detached-server

## Timestamp

2026-05-04 04:36:43 Asia/Seoul

## Environment

- Repository: `/mnt/f/dev/ndx`
- Shell: bash
- Node test runner with built `dist/`
- Docker compose deploy path
- Package version: `0.1.19`

## Executed Steps

1. Reviewed the repeated Windows timeout:
   `timed out waiting for detached ndx server: ws://127.0.0.1:45123`.
2. Confirmed the Windows diagnostic directory under the mapped user home was
   present without `managed-server.log`, making log-path startup coupling a
   remaining plausible blocker.
3. Changed the encoded Windows PowerShell launcher so diagnostic writes remain
   best-effort and the server body is invoked directly.
4. Removed server stdout/stderr redirection to `managed-server.log` from the
   launcher payload.
5. Ran `yarn build`.
6. Ran `node --test dist/tests/cli-workspace.test.js`.
7. Ran `yarn test`.
8. Ran `npm run deploy`.

## Observed Logs

- `yarn build`: passed.
- Targeted test: 8 tests passed, including Windows launcher coverage confirming
  `& $config.exe @argv` and absence of `*>> $config.logPath`.
- `yarn test`: 74 tests passed.
- `npm run deploy`: completed build, full tests, `docker compose down`,
  no-cache `hika00/ndx-sandbox:0.1.1` build, compose startup,
  `/workspace/tmp/ndx-docker-verify.txt` write, and compose teardown.

## Failures

None in local build, targeted test, full test, or deploy verification.

## Reproducibility

Run from `/mnt/f/dev/ndx`:

```bash
yarn build
node --test dist/tests/cli-workspace.test.js
yarn test
npm run deploy
```

Actual Windows detached process survival still needs confirmation from a real
Windows PowerShell install. If startup times out again, the next isolating check
is to run `ndxserver` directly in PowerShell with the same listen ports and
capture its foreground error.

## Locator Outcome

Not applicable; this behavior has no browser surface.
