# Test Report: managed-cli-detached-server

## Timestamp

2026-05-04 14:14:23 Asia/Seoul

## Environment

- Repository: `/mnt/f/dev/ndx`
- Shell: bash
- Node test runner with built `dist/`
- Docker compose deploy path
- Package version: `0.1.23`

## Executed Steps

1. Reviewed Windows logs showing the hidden PowerShell launcher exits early and
   produces an empty parent-captured host log.
2. Changed Windows detached launch from hidden PowerShell to direct hidden
   detached `node.exe` with `serve` args.
3. Changed plain Windows `ndxserver` from foreground server mode to a background
   managed-server trigger while keeping `ndxserver serve` as foreground mode.
4. Updated CLI docs and managed startup test plan.
5. Ran `yarn exec prettier --write`.
6. Ran `yarn build`.
7. Ran `node --test dist/tests/cli-workspace.test.js`.
8. Ran `yarn test`.
9. Ran `npm run deploy`.

## Observed Logs

- Formatting completed for the touched source, docs, plan, and test files.
- `yarn build`: passed.
- Targeted test: 9 tests passed, including Windows launcher coverage for
  `windows-service-trigger-node`, direct `node.exe` command, `serve` args,
  detached mode, hidden window mode, and host log path.
- `yarn test`: 75 tests passed.
- `npm run deploy`: completed build, full tests, `docker compose down`,
  no-cache `hika00/ndx-sandbox:0.1.1` build, compose startup,
  `/workspace/tmp/ndx-docker-verify.txt` write, and compose teardown.

## Failures

None in local build, targeted test, full test, or deploy verification.

## Reproducibility

Run from `/mnt/f/dev/ndx`:

```bash
yarn exec prettier --write README.md docs/usage.md docs/architecture.md docs/internals.md package.json src/cli/main.ts src/cli/workspace.ts tests/cli-workspace.test.ts tests/plans/managed-cli-detached-server-20260504.md tests/plans/verdaccio-managed-server-release-20260504-023.md
yarn build
node --test dist/tests/cli-workspace.test.js
yarn test
npm run deploy
```

On Windows, run plain `ndxserver` to trigger the background managed server. Run
`ndxserver serve` only when a foreground diagnostic terminal is desired.

## Locator Outcome

Not applicable; this behavior has no browser surface.
