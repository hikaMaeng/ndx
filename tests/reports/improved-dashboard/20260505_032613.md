# Test Report: improved-dashboard
## Timestamp
2026-05-05 03:26:13 Asia/Seoul

## Environment
- Branch: `improvedashboard`
- Workspace: `/mnt/c/Users/hika0/.codex/worktrees/03b3/ndx`
- Runtime: Node.js 22, Yarn 4.14.1, Docker compose
- Browser: Playwright Chromium from `hika00/ndx-sandbox:0.1.1`

## Executed Steps
1. Ran `yarn install --immutable`.
2. Ran `yarn build`.
3. Ran `yarn test`.
4. Ran `npm run deploy`.
5. Ran a Docker-hosted Playwright Chromium script against a local HTTP fixture
   that served the built dashboard HTML and dashboard JSON APIs.

## Observed Logs
- `yarn install --immutable`: completed with the expected Yarn PnP ESM loader
  warning.
- `yarn build`: completed successfully.
- `yarn test`: `1..76`, `pass 76`, `fail 0`.
- `npm run deploy`: build and test completed, Docker compose removed prior
  resources, rebuilt `hika00/ndx-sandbox:0.1.1` with no cache, started
  `ndx-ndx-sandbox-1`, wrote the sandbox verification file, then removed the
  container and `ndx_default` network.
- Browser script output:

```json
{"ok":true,"title":"NDX Dashboard","url":"http://127.0.0.1:34879/dashboard"}
```

## Failures
- First browser attempt failed because Playwright was imported as an ESM named
  export from a CommonJS module.
- Second browser attempt reached the target views but failed on an intentionally
  broad `getByText("alice")` strict locator because `alice` appeared in filters,
  session rows, detail metadata, and the Users table.
- Final browser attempt used a scoped Users table locator and passed.

## Reproducibility
Run the commands above from the repository root after `yarn install
--immutable`. The browser verification uses the deploy-built sandbox image and
a local HTTP fixture, so it does not depend on an already-running ndx server.

## Locator Outcome
- Passed: `main` named `Server Dashboard`.
- Passed: `nav` named `Dashboard views`.
- Passed: buttons named `Session Logs` and `Users`.
- Passed: `data-testid="dashboard-server-stats"`.
- Passed: `data-testid="dashboard-session-logs"` and
  `data-testid="session-log-table"`.
- Passed: opening a session replaced the visible right-main view with
  `data-testid="session-log-detail"` and `data-testid="session-log-events"`.
- Passed: `data-testid="dashboard-users"` and scoped
  `data-testid="users-table"` row/cell lookup.
