# Tool Registry Port Report

## Timestamp

2026-04-29T00:33:53+09:00

## Environment

- Workspace: `/mnt/f/dev/ndx`
- Command: `npm test`
- Browser verification: not applicable; no frontend UI.

## Executed Steps

1. Built TypeScript with `tsc -p tsconfig.json`.
2. Ran Node test suite with `node --test dist/tests/*.test.js`.
3. Verified mock shell agent behavior.
4. Verified registry default tool exposure.
5. Verified configured MCP and plugin tool exposure.
6. Ran `npm run deploy` after pushing `codex/runtime-event-protocol`.
7. Verified Docker compose removed prior containers, rebuilt `ndx-agent:local` from the pushed branch, ran in-container tests, executed the mock agent in `/workspace`, and removed the compose network.
8. Verified parallel shell tool calls use separate worker Node processes by comparing each child shell's `$PPID`.

## Observed Logs

- Tests: 9
- Pass: 9
- Fail: 0
- Duration: 655.243595 ms
- Final local test rerun: 9 pass, 0 fail, 571.171363 ms.
- Docker in-container test run: 9 pass, 0 fail, 106.287886 ms.
- Docker mock agent: emitted `[tool:shell]`, wrote `tmp/ndx-docker-verify.txt`, returned `stdout: "verified"`, `exitCode: 0`, `timedOut: false`.
- Parallel worker process test run: 10 pass, 0 fail, 1016.811832 ms.

## Failures

None.

## Reproducibility

Run `npm test` or `npm run deploy` from `/mnt/f/dev/ndx`.

## Residual Risk

MCP command-backed stdio calls and plugin command execution are covered structurally by schema exposure, not by an external live server fixture in this run.
