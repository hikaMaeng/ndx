# Test Report: model-process-bootstrap

## Timestamp

2026-04-29 11:37:46 +0900

## Environment

- Repository: `/mnt/f/dev/ndx`
- Shell: bash
- Node: project default through npm scripts

## Executed Steps

1. Ran `npm run build`.
2. Ran `npm test`.
3. Committed and pushed `5233935` to `origin/codex/runtime-event-protocol`.
4. Ran `npm run deploy` after the push so Docker cloned the branch containing this change.

## Observed Logs

- `npm run build`: `tsc -p tsconfig.json` completed with exit code 0.
- `npm test`: `npm run build && node --test dist/tests/*.test.js` completed with exit code 0.
- TAP summary: 22 tests, 22 pass, 0 fail, duration about 5.0s.
- `npm run deploy`: local build completed, compose removed prior containers, Docker built `ndx-agent:local` from `https://github.com/hikaMaeng/ndx.git@codex/runtime-event-protocol`, in-container `npm test` passed with 22 tests, mock agent wrote `tmp/ndx-docker-verify.txt`, and compose network was removed.

## Failures

No failures in the recorded build, test, or deploy run.

## Reproducibility

Run from `/mnt/f/dev/ndx`:

```bash
npm run build
npm test
```

```bash
npm run deploy
```

## Locator Outcome

No browser UI exists; browser verification was not applicable.
