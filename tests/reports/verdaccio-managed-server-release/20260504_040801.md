# Test Report: verdaccio-managed-server-release-018

## Timestamp

2026-05-04 04:08:01 Asia/Seoul

## Environment

- Repository: `/mnt/f/dev/ndx`
- Registry: `https://verdaccio.neurondev.net/`
- Shell: bash
- Package version: `0.1.18`

## Executed Steps

1. Confirmed Verdaccio latest before publish was `0.1.17`.
2. Ran `npm pack --dry-run --registry https://verdaccio.neurondev.net/`.
3. Published with `npm publish --registry https://verdaccio.neurondev.net/`.
4. Confirmed Verdaccio latest after publish was `0.1.18`.
5. Installed `@neurondev/ndx@0.1.18` into `/tmp/ndx-install-verify-018`.
6. Verified installed `ndx --version` and `ndxserver --version`.

## Observed Logs

- Before publish, `npm view @neurondev/ndx version` returned `0.1.17`.
- `npm pack --dry-run`: package `@neurondev/ndx@0.1.18`, tarball
  `neurondev-ndx-0.1.18.tgz`, shasum
  `e3554b5393d4077dc648126e467ea59fe048dfb8`, 181 files.
- `npm publish`: completed with `+ @neurondev/ndx@0.1.18`.
- After publish, `npm view @neurondev/ndx version` returned `0.1.18`.
- Isolated install: `added 1 package in 4s`.
- Installed binaries:
  - `ndx --version`: `0.1.18`
  - `ndxserver --version`: `0.1.18`

## Failures

None.

## Reproducibility

```bash
npm view @neurondev/ndx version --registry https://verdaccio.neurondev.net/
npm pack --dry-run --registry https://verdaccio.neurondev.net/
npm publish --registry https://verdaccio.neurondev.net/
rm -rf /tmp/ndx-install-verify-018
npm install -g @neurondev/ndx@0.1.18 --registry https://verdaccio.neurondev.net/ --prefix /tmp/ndx-install-verify-018
/tmp/ndx-install-verify-018/bin/ndx --version
/tmp/ndx-install-verify-018/bin/ndxserver --version
```

## Locator Outcome

Not applicable; this release has no browser surface.
