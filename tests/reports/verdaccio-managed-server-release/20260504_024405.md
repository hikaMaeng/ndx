# Test Report: verdaccio-managed-server-release

## Timestamp

2026-05-04 02:44:05 Asia/Seoul

## Environment

- Repository: `/mnt/f/dev/ndx`
- Registry: `https://verdaccio.neurondev.net/`
- Shell: bash
- Package version: `0.1.16`

## Executed Steps

1. Confirmed Verdaccio latest before publish was `0.1.15`.
2. Ran `npm pack --dry-run --registry https://verdaccio.neurondev.net/`.
3. Published with `npm publish --registry https://verdaccio.neurondev.net/`.
4. Confirmed Verdaccio latest after publish was `0.1.16`.
5. Installed `@neurondev/ndx@0.1.16` into `/tmp/ndx-install-verify-016`.
6. Verified installed `ndx --version` and `ndxserver --version`.

## Observed Logs

- Before publish, `npm view @neurondev/ndx version` returned `0.1.15`.
- `npm pack --dry-run`: package `@neurondev/ndx@0.1.16`, tarball
  `neurondev-ndx-0.1.16.tgz`, shasum
  `a51256670cdc8f7def7308581b2b984107be26cd`, 181 files.
- `npm publish`: completed with `+ @neurondev/ndx@0.1.16`.
- After publish, `npm view @neurondev/ndx version` returned `0.1.16`.
- Isolated install: `added 1 package in 3s`.
- Installed binaries:
  - `ndx --version`: `0.1.16`
  - `ndxserver --version`: `0.1.16`

## Failures

None.

## Reproducibility

```bash
npm view @neurondev/ndx version --registry https://verdaccio.neurondev.net/
npm pack --dry-run --registry https://verdaccio.neurondev.net/
npm publish --registry https://verdaccio.neurondev.net/
rm -rf /tmp/ndx-install-verify-016
npm install -g @neurondev/ndx@0.1.16 --registry https://verdaccio.neurondev.net/ --prefix /tmp/ndx-install-verify-016
/tmp/ndx-install-verify-016/bin/ndx --version
/tmp/ndx-install-verify-016/bin/ndxserver --version
```

## Locator Outcome

Not applicable; this release has no browser surface.
