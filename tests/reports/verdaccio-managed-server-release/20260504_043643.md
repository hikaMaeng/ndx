# Test Report: verdaccio-managed-server-release-019

## Timestamp

2026-05-04 04:36:43 Asia/Seoul

## Environment

- Repository: `/mnt/f/dev/ndx`
- Registry: `https://verdaccio.neurondev.net/`
- Shell: bash
- Package version: `0.1.19`

## Executed Steps

1. Confirmed Verdaccio latest before publish was `0.1.18`.
2. Ran `npm pack --dry-run --registry https://verdaccio.neurondev.net/`.
3. Published with `npm publish --registry https://verdaccio.neurondev.net/`.
4. Confirmed Verdaccio latest after publish was `0.1.19`.
5. Installed `@neurondev/ndx@0.1.19` into `/tmp/ndx-install-verify-019`.
6. Verified installed `ndx --version` and `ndxserver --version`.

## Observed Logs

- Before publish, `npm view @neurondev/ndx version` returned `0.1.18`.
- `npm pack --dry-run`: package `@neurondev/ndx@0.1.19`, tarball
  `neurondev-ndx-0.1.19.tgz`, shasum
  `de2317126fc8b8b0d37659cfaffa4cd1d3a22981`, 181 files.
- `npm publish`: completed with `+ @neurondev/ndx@0.1.19`.
- After publish, `npm view @neurondev/ndx version` returned `0.1.19`.
- Isolated install: `added 1 package in 3s`.
- Installed binaries:
  - `ndx --version`: `0.1.19`
  - `ndxserver --version`: `0.1.19`

## Failures

None.

## Reproducibility

```bash
npm view @neurondev/ndx version --registry https://verdaccio.neurondev.net/
npm pack --dry-run --registry https://verdaccio.neurondev.net/
npm publish --registry https://verdaccio.neurondev.net/
rm -rf /tmp/ndx-install-verify-019
npm install -g @neurondev/ndx@0.1.19 --registry https://verdaccio.neurondev.net/ --prefix /tmp/ndx-install-verify-019
/tmp/ndx-install-verify-019/bin/ndx --version
/tmp/ndx-install-verify-019/bin/ndxserver --version
```

## Locator Outcome

Not applicable; this release has no browser surface.
