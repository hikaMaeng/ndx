# Test Report: verdaccio-managed-server-release-017

## Timestamp

2026-05-04 03:20:59 Asia/Seoul

## Environment

- Repository: `/mnt/f/dev/ndx`
- Registry: `https://verdaccio.neurondev.net/`
- Shell: bash
- Package version: `0.1.17`

## Executed Steps

1. Confirmed Verdaccio latest before publish was `0.1.16`.
2. Ran `npm pack --dry-run --registry https://verdaccio.neurondev.net/`.
3. Published with `npm publish --registry https://verdaccio.neurondev.net/`.
4. Confirmed Verdaccio latest after publish was `0.1.17`.
5. Installed `@neurondev/ndx@0.1.17` into `/tmp/ndx-install-verify-017`.
6. Verified installed `ndx --version` and `ndxserver --version`.

## Observed Logs

- Before publish, `npm view @neurondev/ndx version` returned `0.1.16`.
- `npm pack --dry-run`: package `@neurondev/ndx@0.1.17`, tarball
  `neurondev-ndx-0.1.17.tgz`, shasum
  `50f4a8ebadce1c921867d8db52fa219c084980f8`, 181 files.
- `npm publish`: completed with `+ @neurondev/ndx@0.1.17`.
- After publish, `npm view @neurondev/ndx version` returned `0.1.17`.
- Isolated install: `added 1 package in 3s`.
- Installed binaries:
  - `ndx --version`: `0.1.17`
  - `ndxserver --version`: `0.1.17`

## Failures

None.

## Reproducibility

```bash
npm view @neurondev/ndx version --registry https://verdaccio.neurondev.net/
npm pack --dry-run --registry https://verdaccio.neurondev.net/
npm publish --registry https://verdaccio.neurondev.net/
rm -rf /tmp/ndx-install-verify-017
npm install -g @neurondev/ndx@0.1.17 --registry https://verdaccio.neurondev.net/ --prefix /tmp/ndx-install-verify-017
/tmp/ndx-install-verify-017/bin/ndx --version
/tmp/ndx-install-verify-017/bin/ndxserver --version
```

## Locator Outcome

Not applicable; this release has no browser surface.
