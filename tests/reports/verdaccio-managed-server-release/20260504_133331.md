# Test Report: verdaccio-managed-server-release-022

## Timestamp

2026-05-04 13:33:31 Asia/Seoul

## Environment

- Repository: `/mnt/f/dev/ndx`
- Registry: `https://verdaccio.neurondev.net/`
- Shell: bash
- Package version: `0.1.22`

## Executed Steps

1. Confirmed Verdaccio latest before publish was `0.1.21`.
2. Ran `npm pack --dry-run --registry https://verdaccio.neurondev.net/`.
3. Published with `npm publish --registry https://verdaccio.neurondev.net/`.
4. Confirmed Verdaccio latest after publish was `0.1.22`.
5. Installed `@neurondev/ndx@0.1.22` into `/tmp/ndx-install-verify-022`.
6. Verified installed `ndx --version` and `ndxserver --version`.

## Observed Logs

- Before publish, `npm view @neurondev/ndx version` returned `0.1.21`.
- `npm pack --dry-run`: package `@neurondev/ndx@0.1.22`, tarball
  `neurondev-ndx-0.1.22.tgz`, shasum
  `8fd1be6235064695351a65e99887ea77efc85915`, 181 files.
- `npm publish`: completed with `+ @neurondev/ndx@0.1.22`.
- After publish, `npm view @neurondev/ndx version` returned `0.1.22`.
- Isolated install: `added 1 package in 3s`.
- Installed binaries:
  - `ndx --version`: `0.1.22`
  - `ndxserver --version`: `0.1.22`

## Failures

None.

## Reproducibility

```bash
npm view @neurondev/ndx version --registry https://verdaccio.neurondev.net/
npm pack --dry-run --registry https://verdaccio.neurondev.net/
npm publish --registry https://verdaccio.neurondev.net/
rm -rf /tmp/ndx-install-verify-022
npm install -g @neurondev/ndx@0.1.22 --registry https://verdaccio.neurondev.net/ --prefix /tmp/ndx-install-verify-022
/tmp/ndx-install-verify-022/bin/ndx --version
/tmp/ndx-install-verify-022/bin/ndxserver --version
```

## Locator Outcome

Not applicable; this release has no browser surface.
