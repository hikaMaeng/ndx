# Test Report: verdaccio-managed-server-release-020

## Timestamp

2026-05-04 05:05:06 Asia/Seoul

## Environment

- Repository: `/mnt/f/dev/ndx`
- Registry: `https://verdaccio.neurondev.net/`
- Shell: bash
- Package version: `0.1.20`

## Executed Steps

1. Confirmed Verdaccio latest before publish was `0.1.19`.
2. Ran `npm pack --dry-run --registry https://verdaccio.neurondev.net/`.
3. Published with `npm publish --registry https://verdaccio.neurondev.net/`.
4. Confirmed Verdaccio latest after publish was `0.1.20`.
5. Installed `@neurondev/ndx@0.1.20` into `/tmp/ndx-install-verify-020`.
6. Verified installed `ndx --version` and `ndxserver --version`.

## Observed Logs

- Before publish, `npm view @neurondev/ndx version` returned `0.1.19`.
- `npm pack --dry-run`: package `@neurondev/ndx@0.1.20`, tarball
  `neurondev-ndx-0.1.20.tgz`, shasum
  `ed651ee594805d7e0f180f0ba1e6d7d0544485c9`, 181 files.
- `npm publish`: completed with `+ @neurondev/ndx@0.1.20`.
- After publish, `npm view @neurondev/ndx version` returned `0.1.20`.
- Isolated install: `added 1 package in 4s`.
- Installed binaries:
  - `ndx --version`: `0.1.20`
  - `ndxserver --version`: `0.1.20`

## Failures

None.

## Reproducibility

```bash
npm view @neurondev/ndx version --registry https://verdaccio.neurondev.net/
npm pack --dry-run --registry https://verdaccio.neurondev.net/
npm publish --registry https://verdaccio.neurondev.net/
rm -rf /tmp/ndx-install-verify-020
npm install -g @neurondev/ndx@0.1.20 --registry https://verdaccio.neurondev.net/ --prefix /tmp/ndx-install-verify-020
/tmp/ndx-install-verify-020/bin/ndx --version
/tmp/ndx-install-verify-020/bin/ndxserver --version
```

## Locator Outcome

Not applicable; this release has no browser surface.
