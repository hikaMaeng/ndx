# Test Report: verdaccio-managed-server-release-023

## Timestamp

2026-05-04 14:14:23 Asia/Seoul

## Environment

- Repository: `/mnt/f/dev/ndx`
- Registry: `https://verdaccio.neurondev.net/`
- Shell: bash
- Package version: `0.1.23`

## Executed Steps

1. Confirmed Verdaccio latest before publish was `0.1.22`.
2. Ran `npm pack --dry-run --registry https://verdaccio.neurondev.net/`.
3. Published with `npm publish --registry https://verdaccio.neurondev.net/`.
4. Confirmed Verdaccio latest after publish was `0.1.23`.
5. Installed `@neurondev/ndx@0.1.23` into `/tmp/ndx-install-verify-023`.
6. Verified installed `ndx --version` and `ndxserver --version`.

## Observed Logs

- Before publish, `npm view @neurondev/ndx version` returned `0.1.22`.
- `npm pack --dry-run`: package `@neurondev/ndx@0.1.23`, tarball
  `neurondev-ndx-0.1.23.tgz`, shasum
  `6d54213f92bfea94da53504eb252f07aa55751cc`, 181 files.
- `npm publish`: completed with `+ @neurondev/ndx@0.1.23`.
- After publish, `npm view @neurondev/ndx version` returned `0.1.23`.
- Isolated install: `added 1 package in 3s`.
- Installed binaries:
  - `ndx --version`: `0.1.23`
  - `ndxserver --version`: `0.1.23`

## Failures

None.

## Reproducibility

```bash
npm view @neurondev/ndx version --registry https://verdaccio.neurondev.net/
npm pack --dry-run --registry https://verdaccio.neurondev.net/
npm publish --registry https://verdaccio.neurondev.net/
rm -rf /tmp/ndx-install-verify-023
npm install -g @neurondev/ndx@0.1.23 --registry https://verdaccio.neurondev.net/ --prefix /tmp/ndx-install-verify-023
/tmp/ndx-install-verify-023/bin/ndx --version
/tmp/ndx-install-verify-023/bin/ndxserver --version
```

## Locator Outcome

Not applicable; this release has no browser surface.
