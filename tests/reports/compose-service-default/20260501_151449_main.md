# Test Report: Main Compose Service Default

## Timestamp

2026-05-01 15:14:49 +0900

## Environment

- Workspace: `/tmp/ndx-main-compose-service-verify`
- Branch: `main`
- Commit: `b19e368672b40d17a7d1fda29c10b59137e28b3b`
- Node.js: `v22.22.2`
- Yarn: `4.14.1`
- Docker image: `ndx-agent:local`

## Executed Steps

1. Fetched `origin/main` after PR #15 was merged.
2. Created a clean main worktree at `/tmp/ndx-main-compose-service-verify`.
3. Ran `yarn install --immutable`.
4. Ran `npm test`.
5. Ran `npm run deploy`.
6. Ran `docker compose up -d ndx-agent`.
7. Waited for the service healthcheck to report `healthy`.
8. Inspected `docker compose logs --tail 120 ndx-agent`.
9. Ran `docker compose ps`.
10. Fetched `http://127.0.0.1:45124/dashboard`.
11. Opened a WebSocket connection to `ws://127.0.0.1:45123`.

## Observed Logs

- `yarn install --immutable`: passed with only the Yarn PnP ESM loader warning.
- `npm test`: 50 tests, 50 passed.
- `npm run deploy`: passed. Docker rebuilt `ndx-agent:local` from
  `https://github.com/hikaMaeng/ndx.git@main`, ran in-container `yarn test`
  with 50 passing tests, ran the mock agent, wrote `verified` to
  `/workspace/tmp/ndx-docker-verify.txt`, and removed the compose network.
- `docker compose up -d ndx-agent`: passed and left the service running.
- Service healthcheck: `healthy`.
- `docker compose ps`: showed `0.0.0.0:45123-45124->45123-45124/tcp`.
- Startup logs included:
  - `[ndx-service] installed_global_settings=/home/.ndx/settings.json`
  - `[ndx-image] git_ref=main`
  - `[ndx-image] git_commit=b19e368672b40d17a7d1fda29c10b59137e28b3b`
  - `[ndx-image] node=v22.22.2 yarn=4.14.1`
  - `[ndx-service] socket_bind=0.0.0.0:45123`
  - `[ndx-service] dashboard_bind=0.0.0.0:45124`
  - `[ndx-service] socket_url=ws://127.0.0.1:45123`
  - `[ndx-service] dashboard_url=http://127.0.0.1:45124`
  - CLI welcome ASCII art before listener readiness lines.
  - `[session-server] ws://0.0.0.0:45123`
  - `[dashboard] http://0.0.0.0:45124`
- Dashboard fetch found `ndx Agent Service`, `role="status"`, and
  `data-testid="agent-dashboard-placeholder"`.
- WebSocket open check to `ws://127.0.0.1:45123`: passed.

## Failures

None.

## Reproducibility

```bash
yarn install --immutable
npm test
npm run deploy
docker compose up -d ndx-agent
docker compose logs --tail 120 ndx-agent
docker compose ps
curl -fsS http://127.0.0.1:45124/dashboard
node -e "const ws = new WebSocket('ws://127.0.0.1:45123'); ws.addEventListener('open', () => { console.log('websocket open'); ws.close(); });"
```
