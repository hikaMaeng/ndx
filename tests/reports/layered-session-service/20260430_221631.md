# Test Report: layered-session-service

## Timestamp

2026-04-30 22:16:31 +0900

## Environment

- Workspace: `/mnt/f/dev/ndx`
- Branch: `codex/layeredarchitecture`
- Runtime: local Node.js test runner
- Docker: local Docker Compose

## Executed Steps

1. Ran `npm test`.
2. Verified the added config, CLI, and session-server coverage is included in
   the full suite.
3. Pushed `codex/layeredarchitecture` so Docker could clone the branch.
4. Ran `npm run deploy`.
5. Started an ephemeral `SessionServer` from `dist/` with a temp global dir and
   fetched `/dashboard` to verify the dashboard placeholder contract.

## Observed Logs

- `npm test` completed with `1..50`.
- Pass: `50`
- Fail: `0`
- Added passing subtest:
  `session server exposes account methods, client identity, and dashboard placeholder`.
- `npm run deploy` completed successfully:
  - local TypeScript build completed;
  - `docker compose down --remove-orphans` removed the prior target stack;
  - image `ndx-agent:local` built from
    `https://github.com/hikaMaeng/ndx.git@codex/layeredarchitecture`;
  - in-container `npm test` completed with `1..50`, pass `50`, fail `0`;
  - in-container mock agent wrote `verified` to
    `tmp/ndx-docker-verify.txt`;
  - final `docker compose down --remove-orphans` removed the compose network.
- Ephemeral dashboard fetch returned:

```json
{ "status": 200, "hasMain": true, "hasStatus": true, "hasTestId": true }
```

## Failures

None in local `npm test` or `npm run deploy`.

## Reproducibility

Run from repository root:

```bash
npm test
npm run deploy
```

## Locator Outcome

The dashboard placeholder was verified through the session-server test and a
post-deploy ephemeral server fetch. Both asserted the stable placeholder
contract: `main aria-labelledby="dashboard-title"`, `role="status"`, and
`data-testid="agent-dashboard-placeholder"`.

Browser Use was unavailable in this session because tool discovery did not
expose the required Node REPL `js` tool. A temporary `npx playwright` fallback
also did not complete package resolution before it was stopped, so no visual
screenshot was captured.
