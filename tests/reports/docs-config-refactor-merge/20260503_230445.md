# Test Report: docs-config-refactor-merge

## Timestamp

2026-05-03 23:04:45 +0900

## Environment

- Workspace: `/mnt/f/dev/ndx`
- Shell: bash
- Branch: `main`
- Merge source: `codex/docs-config-refactor`
- Docker image built by deploy: `hika00/ndx-sandbox:0.1.1`

## Executed Steps

1. Fast-forwarded local `main` to `origin/main`.
2. Merged `codex/docs-config-refactor`; resolved documentation and TypeScript
   conflicts.
3. Ran `yarn build`: passed.
4. Ran initial `npm run deploy`: failed during `yarn test` because centralized
   defaults temporarily exposed `hika00/ndx-sandbox:0.1.0` while current tests
   and compose require `hika00/ndx-sandbox:0.1.1`.
5. Updated `src/config/defaults.ts`, `docs/usage.md`, and
   `docs/constraints.md` to `hika00/ndx-sandbox:0.1.1`.
6. Ran `yarn build && node --test dist/tests/tool-orchestration.test.js`:
   passed, 2 tests passed.
7. Reran `npm run deploy`: passed.

## Observed Logs

- `yarn build`: exit 0.
- Focused tool orchestration run: `# pass 2`, `# fail 0`.
- Full deploy test phase: `# tests 70`, `# pass 70`, `# fail 0`.
- Docker phase built `docker.io/hika00/ndx-sandbox:0.1.1`.
- Compose created and started `ndx-ndx-sandbox-1`, executed the sandbox write
  verification, then stopped and removed the container and `ndx_default`
  network.

## Failures

- First full deploy attempt failed before Docker compose because the merge
  resolution took the stale `0.1.0` centralized default. The final staged result
  uses `0.1.1` and passed deploy.

## Reproducibility

Run from `/mnt/f/dev/ndx`:

```bash
yarn build
node --test dist/tests/tool-orchestration.test.js
npm run deploy
```

## Locator Outcome

Not applicable. No browser verification was executed for this merge-only
verification.
