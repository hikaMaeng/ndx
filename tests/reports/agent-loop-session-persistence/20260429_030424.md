# Test Report: agent-loop-session-persistence

## Timestamp

2026-04-29 03:04:24 KST

## Environment

- Workspace: `/mnt/f/dev/ndx`
- Shell: bash
- Verification commands:
  - `rg -n "CodexThread|RolloutRecorder|history\\.jsonl|ThreadStateManager|thread/read|WebSocket|Codex::spawn|Session::record_conversation_items|Session::send_event" codex-rs/core/docs/agent-loop.md docs/agent-loop.md`
  - `git diff --check -- codex-rs/core/docs/agent-loop.md docs/agent-loop.md tests/plans/agent-loop-session-persistence-20260429.md`
  - `npm run deploy`

## Executed Steps

1. Inspected Rust core live thread/session files:
   `codex-rs/core/src/codex_thread.rs`,
   `codex-rs/core/src/thread_manager.rs`, and
   `codex-rs/core/src/session/mod.rs`.
2. Inspected persistence files:
   `codex-rs/rollout/src/recorder.rs`,
   `codex-rs/rollout/src/state_db.rs`, and
   `codex-rs/core/src/message_history.rs`.
3. Inspected app-server subscription/read paths:
   `codex-rs/app-server/src/thread_state.rs`,
   `codex-rs/app-server/src/codex_message_processor.rs`, and
   `codex-rs/app-server-protocol/src/protocol/thread_history.rs`.
4. Updated `codex-rs/core/docs/agent-loop.md` with live
   thread/session, rollout JSONL, history JSONL, state DB, app-server
   subscription, and `thread/read` reconstruction details.
5. Updated `docs/agent-loop.md` to state the TypeScript runtime gap relative
   to Rust Codex.
6. Added `tests/plans/agent-loop-session-persistence-20260429.md`.
7. Ran marker search and whitespace validation.
8. Ran `npm run deploy`.

## Observed Logs

- Marker search found all required concepts:
  `CodexThread`, `RolloutRecorder`, `history.jsonl`, `ThreadStateManager`,
  `thread/read`, `WebSocket`, `Codex::spawn`,
  `Session::record_conversation_items`, and `Session::send_event`.
- `git diff --check`: passed with no whitespace errors.
- `npm run deploy`: passed.
- Container test summary during deploy: `# tests 11`, `# pass 11`,
  `# fail 0`.
- Mock CLI verification returned `mock agent completed`.
- Docker Compose network was removed during cleanup.

## Failures

None.

## Reproducibility

- Re-run the marker search command above to verify documentation coverage.
- Re-run `npm run deploy` from `/mnt/f/dev/ndx` to verify build, Docker,
  container tests, mock CLI execution, and cleanup.
- Note: deploy clones the current GitHub branch in the container. The local
  documentation update is verified by marker search and `git diff --check`.

## Locator Outcome

Not applicable. This change has no browser-rendered UI.
