{
  "name": "@neurondev/ndx",
  "version": "0.1.17",
  "type": "module",
  "description": "TypeScript-first local coding agent.",
  "files": [
    "dist/src",
    "README.md",
    "LICENSE",
    "NOTICE"
  ],
  "bin": {
    "ndx": "dist/src/cli/main.js",
    "ndxserver": "dist/src/cli/main.js"
  },
  "scripts": {
    "build": "yarn exec tsc -p tsconfig.json",
    "test": "yarn build && node --test dist/tests/*.test.js",
    "start": "node dist/src/cli/main.js",
    "deploy": "yarn build && yarn test && docker compose down --remove-orphans && docker compose build --no-cache ndx-sandbox && docker compose up -d ndx-sandbox && docker compose exec -T ndx-sandbox bash -lc \"mkdir -p /workspace/tmp && echo verified > /workspace/tmp/ndx-docker-verify.txt\" && docker compose down --remove-orphans"
  },
  "devDependencies": {
    "@types/node": "^20.19.18",
    "prettier": "^3.5.3",
    "typescript": "^5.9.2"
  },
  "engines": {
    "node": ">=22",
    "yarn": ">=4.14.1"
  },
  "packageManager": "yarn@4.14.1"
}
